# 백엔드 자동 머징 영향도 예측 (98개 파일)

**작성일**: 2025-11-03
**분석 대상**: src/ 디렉토리의 CARET MODIFICATION 파일 98개
**시나리오**: Cline v3.32.7 → v3.34.0 자동 머징 시도 시 예상 결과

---

## 🎯 전체 예측 요약

| 충돌 위험도 | 파일 수 | 비율 | 자동 해결 가능 | 수동 개입 필요 |
|-------------|---------|------|----------------|----------------|
| ⚠️ **매우 높음** | 32개 | 32.7% | 0% | 100% |
| 🟡 **높음** | 28개 | 28.6% | 20% | 80% |
| 🟠 **중간** | 24개 | 24.5% | 60% | 40% |
| 🟢 **낮음** | 14개 | 14.3% | 100% | 0% |
| **전체** | **98개** | **100%** | **32.7%** | **67.3%** |

### 예상 결과
- **자동 성공**: 32개 (32.7%) - 충돌 없이 자동 머징 가능
- **수동 해결**: 66개 (67.3%) - 개발자 개입 필요
- **예상 작업 시간**: 8-16시간 (수동 해결 기준)

---

## 📊 카테고리별 상세 예측

### 1️⃣ 자동생성 Proto 파일 (14개) - 🟢 낮음

**파일 목록**:
```
src/generated/nice-grpc/cline/*.ts (4개)
src/generated/nice-grpc/caret/*.ts (3개)
src/generated/grpc-js/cline/*.ts (4개)
src/generated/grpc-js/caret/*.ts (3개)
```

**충돌 예측**:
- **충돌 확률**: 0% ✅
- **자동 해결**: 100% ✅
- **영향 범위**: 없음

**이유**:
```bash
# 이 파일들은 npm run protos 실행 시 재생성됨
npm run protos  # proto/*.proto 기반으로 완전 재생성

# Cline v3.34.0의 proto 정의 + Caret의 proto 정의 = 자동 생성
# 수동 수정이 없으므로 충돌 불가능
```

**자동 머징 전략**:
1. Cline v3.34.0 병합
2. `npm run protos` 실행
3. 자동 생성 완료 ✅

**위험도**: ⭐☆☆☆☆ (없음)

---

### 2️⃣ Shared 타입 (14개) - 🟠 중간

**파일 목록**:
```
src/shared/proto/cline/*.ts (4개)
src/shared/proto/caret/*.ts (3개)
src/shared/storage/types.ts
src/shared/Languages.ts
src/shared/CaretSettings.ts
src/shared/ExtensionMessage.ts
src/shared/CaretAccount.ts
src/shared/api.ts
src/shared/webview/types.ts
```

**충돌 예측**:
- **충돌 확률**: 50%
- **자동 해결**: 60% (proto 파일들)
- **수동 해결**: 40% (ExtensionMessage.ts, types.ts 등)

#### 2-1. Proto 타입 (7개) - 자동 해결 가능
**파일**: `src/shared/proto/**/*.ts`

**예상 충돌**:
```typescript
// Cline v3.34.0 추가
export interface NewClineMessage { ... }

// Caret 추가 (CARET MODIFICATION)
export interface CaretMessage { ... }

// 충돌: 없음 (서로 다른 타입 추가)
// 자동 머징: 성공 ✅
```

**위험도**: ⭐⭐☆☆☆ (낮음)

---

#### 2-2. ExtensionMessage.ts - **충돌 확률 90%** ⚠️

**예상 충돌 시나리오**:
```typescript
// Cline v3.32.7 (base)
export type ExtensionMessage =
  | { type: "action" }
  | { type: "state" }

// Cline v3.34.0 변경
export type ExtensionMessage =
  | { type: "action" }
  | { type: "state" }
  | { type: "subagentCommand" }  // 신규 추가
  | { type: "cliStatus" }        // 신규 추가

// Caret 변경 (CARET MODIFICATION)
export type ExtensionMessage =
  | { type: "action" }
  | { type: "state" }
  | { type: "caretModeChanged" }     // 신규 추가
  | { type: "personaUpdated" }       // 신규 추가
  | { type: "inputHistoryLoaded" }   // 신규 추가

// 충돌: ❌ 같은 타입 정의를 두 브랜치가 다르게 수정
```

**자동 머징 결과**:
```bash
<<<<<<< HEAD (Caret)
export type ExtensionMessage =
  | { type: "action" }
  | { type: "state" }
  | { type: "caretModeChanged" }
  | { type: "personaUpdated" }
  | { type: "inputHistoryLoaded" }
=======
export type ExtensionMessage =
  | { type: "action" }
  | { type: "state" }
  | { type: "subagentCommand" }
  | { type: "cliStatus" }
>>>>>>> v3.34.0 (Cline)
```

**수동 해결 필요**:
```typescript
// 수동으로 통합 필요
export type ExtensionMessage =
  | { type: "action" }
  | { type: "state" }
  | { type: "subagentCommand" }      // Cline v3.34.0
  | { type: "cliStatus" }            // Cline v3.34.0
  | { type: "caretModeChanged" }     // Caret
  | { type: "personaUpdated" }       // Caret
  | { type: "inputHistoryLoaded" }   // Caret
```

**위험도**: ⭐⭐⭐⭐☆ (높음)
**예상 해결 시간**: 30분-1시간

---

#### 2-3. storage/types.ts - **충돌 확률 70%** ⚠️

**예상 충돌**:
```typescript
// Cline v3.34.0 추가
export interface GlobalState {
  // ... 기존 필드
  cliInstalled?: boolean      // 신규
  subagentsEnabled?: boolean  // 신규
}

// Caret 추가
export interface GlobalState {
  // ... 기존 필드
  caretMode?: "caret" | "cline"  // 신규
  personaId?: string             // 신규
}

// 충돌: 같은 인터페이스를 두 곳에서 확장
```

**위험도**: ⭐⭐⭐⭐☆ (높음)
**예상 해결 시간**: 20분

---

### 3️⃣ Controller (19개) - 🟡 높음

**파일 분류**:
- **Caret 전용**: 13개 (Persona 4개 + Account 9개)
- **공통**: 6개 (state, file, models)

#### 3-1. Caret 전용 Controller (13개) - **충돌 없음** ✅

**파일 목록**:
```
src/core/controller/persona/*.ts (4개)
src/core/controller/caretAccount/*.ts (9개)
```

**충돌 예측**:
- **충돌 확률**: 0% ✅
- **이유**: Cline v3.34.0에 이 파일들이 없음 (Caret 신규 추가)

**자동 머징 결과**: 그대로 유지 ✅

**위험도**: ⭐☆☆☆☆ (없음)

---

#### 3-2. src/core/controller/index.ts - **충돌 확률 95%** ⚠️⚠️

**예상 충돌 이유**:
1. **Import 충돌**: Cline v3.34.0에서 CLI 관련 import 추가
2. **초기화 로직 충돌**: Caret의 CaretGlobalManager vs Cline의 CLI 초기화
3. **메시지 핸들러 충돌**: 새로운 메시지 타입 처리 추가

**예상 충돌 영역**:
```typescript
// 1. Import 섹션 충돌
// Cline v3.34.0 추가
import { detectSubagentCommand } from '@/integrations/cli-subagents'
import { CliStatusChecker } from '@/services/cli'

// Caret 추가 (CARET MODIFICATION)
import { CaretGlobalManager } from '@caret/managers/CaretGlobalManager'
import { FeatureConfig } from '@caret/shared/FeatureConfig'

// 충돌: 같은 위치에 다른 import 추가
```

```typescript
// 2. 초기화 로직 충돌
// Cline v3.34.0
async function initialize() {
  // CLI 상태 체크
  const cliInstalled = await checkCliInstallation()
  if (cliInstalled) {
    await initializeSubagentSystem()
  }
}

// Caret (CARET MODIFICATION)
async function initialize() {
  // Caret 설정 로드
  const featureConfig = await FeatureConfig.load()
  const inputHistory = await CaretGlobalManager.getInputHistory()
}

// 충돌: 같은 함수를 다르게 수정
```

**수동 해결 필요**:
- Import 통합 (약 10개 import)
- 초기화 로직 순서 조정
- 메시지 핸들러 통합

**위험도**: ⭐⭐⭐⭐⭐ (매우 높음)
**예상 해결 시간**: 2-3시간
**영향 범위**: 전체 시스템 (메시지 라우팅 중심)

---

#### 3-3. 기타 Controller (5개) - **충돌 확률 40%** 🟠

**파일 목록**:
```
src/core/controller/state/updateSettings.ts
src/core/controller/file/toggleCaretRule.ts
src/core/controller/file/createRuleFile.ts
src/core/controller/file/refreshRules.ts
src/core/controller/models/updateApiConfigurationProto.ts
```

**예상 충돌**:
- **updateSettings.ts**: Cline이 CLI 설정 추가, Caret이 mode 설정 추가 → 충돌 가능
- **규칙 파일들**: Caret 전용 → 충돌 없음 ✅
- **updateApiConfigurationProto.ts**: Proto 필드 추가 → 중간 충돌

**위험도**: ⭐⭐⭐☆☆ (중간)
**예상 해결 시간**: 1시간

---

### 4️⃣ Core - Task & Prompts (30개) - ⚠️ **매우 높음**

#### 4-1. src/core/task/index.ts - **충돌 확률 99%** ⚠️⚠️⚠️

**최고 위험 파일**:
- **수정 횟수**: Caret에서 20+회 수정
- **Cline v3.34.0 변경**: CLI Subagent 시스템 추가
- **충돌 예상 지점**: 5곳 이상

**예상 충돌 시나리오 1: Import**
```typescript
// Cline v3.34.0 추가
import { SubagentTerminalManager } from '@/integrations/cli-subagents/terminal'
import { detectSubagentCommand } from '@/integrations/cli-subagents/subagent_command'

// Caret 추가 (CARET MODIFICATION)
import { refreshExternalRulesToggles } from '@/core/context/instructions/user-instructions/external-rules'
import { getBrandName } from '@caret/utils/brand-utils'

// 충돌: ❌ 같은 Import 블록
```

**예상 충돌 시나리오 2: TerminalManager 초기화**
```typescript
// Cline v3.34.0
const isSubagent = await detectSubagentCommand()
const terminalManager = isSubagent
  ? new SubagentTerminalManager()
  : new TerminalManager()

// Caret (CARET MODIFICATION)
const isSubagent = await detectSubagentCommand()
const isCliInstalled = await checkCliInstallation()
const terminalManager = isSubagent && isCliInstalled
  ? new SubagentTerminalManager()
  : new TerminalManager()

// 충돌: 같은 변수를 다르게 정의
```

**예상 충돌 시나리오 3: 규칙 로딩**
```typescript
// Cline v3.34.0: .clinerules만 지원
const rules = await loadExternalRules(cwd, '.clinerules')

// Caret (CARET MODIFICATION): .caretrules 추가
const { localCaretRulesFileInstructions, caretLocalToggles } =
  await refreshExternalRulesToggles(cwd, rules)

// 충돌: 규칙 로딩 로직 완전 상이
```

**예상 충돌 시나리오 4: 시스템 프롬프트 생성**
```typescript
// Cline v3.34.0
const systemPrompt = createSystemPrompt({
  // ... 기존 필드
  cliSubagentsEnabled: isSubagentsEnabled,
  isCliSubagent: isSubagent,
})

// Caret (CARET MODIFICATION)
const systemPrompt = createSystemPrompt({
  // ... 기존 필드
  localCaretRulesFileInstructions,
  isSubagentsEnabledAndCliInstalled,
  isCliSubagent,
  brandName: getBrandName(),
})

// 충돌: 같은 함수 호출, 다른 파라미터
```

**수동 해결 난이도**: ⭐⭐⭐⭐⭐ (매우 어려움)
**예상 해결 시간**: 3-4시간
**영향 범위**: 전체 태스크 실행 시스템
**테스트 필요성**: ⭐⭐⭐⭐⭐ (필수)

---

#### 4-2. Tool Handlers (7개) - **충돌 확률 20%** 🟢

**파일 목록**:
```
src/core/task/tools/handlers/WriteToFileToolHandler.ts
src/core/task/tools/handlers/BrowserToolHandler.ts
src/core/task/tools/handlers/ListFilesToolHandler.ts
src/core/task/tools/handlers/ExecuteCommandToolHandler.ts
src/core/task/tools/handlers/AskFollowupQuestionToolHandler.ts
src/core/task/tools/handlers/ReadFileToolHandler.ts
src/core/task/tools/handlers/ListCodeDefinitionNamesToolHandler.ts
```

**Caret 수정 내용**: 주로 로깅만 변경
```typescript
// CARET MODIFICATION: Brand-aware logging
Logger.info(`[${getBrandName()}] Tool executed`)
```

**Cline v3.34.0 변경 예상**: Tool 로직 개선

**충돌 예측**:
- Cline이 로깅 부분을 건드리지 않으면 충돌 없음 ✅
- 로직만 변경하면 자동 머징 가능

**위험도**: ⭐⭐☆☆☆ (낮음)
**예상 해결 시간**: 30분 (충돌 발생 시)

---

#### 4-3. Prompts (7개) - **충돌 확률 80%** ⚠️

**파일 목록**:
```
src/core/prompts/responses.ts
src/core/prompts/system-prompt/index.ts
src/core/prompts/system-prompt/types.ts
src/core/prompts/system-prompt/templates/placeholders.ts
src/core/prompts/system-prompt/components/index.ts
src/core/prompts/system-prompt/components/cli_subagents.ts
src/core/prompts/system-prompt/components/user_instructions.ts
```

**주요 충돌 지점**:

##### cli_subagents.ts - **충돌 확률 100%** ⚠️⚠️
```typescript
// Cline v3.34.0 (신규 파일)
export function getCliSubagentsComponent(): string {
  return `
# CLI Subagents
You can launch Cline CLI subagents...
  `
}

// Caret (CARET MODIFICATION)
export function getCliSubagentsComponent(): string {
  const brandName = getBrandName()
  return `
# CLI Subagents
You can launch ${brandName} CLI subagents...
  `
}

// 충돌: 같은 파일, 다른 내용
```

**수동 해결**: 브랜드명 변수화 적용 필요

##### types.ts - **충돌 확률 70%**
```typescript
// Cline v3.34.0: CLI 관련 타입 추가
export interface SystemPromptArgs {
  // ... 기존 필드
  cliSubagentsEnabled?: boolean
  isCliSubagent?: boolean
}

// Caret (CARET MODIFICATION)
export interface SystemPromptArgs {
  // ... 기존 필드
  localCaretRulesFileInstructions?: string
  brandName?: string
}

// 충돌: 같은 인터페이스 확장
```

**위험도**: ⭐⭐⭐⭐☆ (높음)
**예상 해결 시간**: 1.5-2시간

---

#### 4-4. Storage (4개) - **충돌 확률 60%** 🟠

**파일 목록**:
```
src/core/storage/disk.ts
src/core/storage/StateManager.ts
src/core/storage/utils/state-helpers.ts
src/core/storage/state-keys.ts
```

**주요 충돌 지점**:

##### state-keys.ts - **충돌 확률 80%** ⚠️
```typescript
// Cline v3.34.0 추가
export const STATE_KEYS = {
  // ... 기존
  CLI_INSTALLED: "cline.cli.installed",
  SUBAGENTS_ENABLED: "cline.subagents.enabled",
}

// Caret (CARET MODIFICATION)
export const CARET_STATE_KEYS = {
  PROMPT_MODE: "caret.promptSystem.mode",
  INPUT_HISTORY: "caret.inputHistory",
  PERSONA: "caret.persona",
}

// 충돌: 같은 객체에 다른 키 추가
```

**수동 해결**: 두 브랜치의 키를 모두 포함

**위험도**: ⭐⭐⭐☆☆ (중간)
**예상 해결 시간**: 1시간

---

#### 4-5. API Providers (6개) - **충돌 확률 30%** 🟢

**파일 목록**:
```
src/core/api/providers/cline.ts
src/core/api/providers/dify.ts
src/core/api/providers/vscode-lm.ts
src/core/api/providers/litellm.ts
src/core/api/providers/__tests__/claude-code.test.ts
src/core/api/transform/stream.ts
```

**Caret 수정**: 주로 헤더 추가
```typescript
// CARET MODIFICATION: Brand-aware headers
headers: {
  'User-Agent': `${getBrandName()}/1.0`
}
```

**Cline v3.34.0 예상 변경**: Provider 로직 개선

**충돌 예측**:
- 헤더 부분은 안전 (다른 영역)
- 로직 개선과 충돌 가능성 낮음

**위험도**: ⭐⭐☆☆☆ (낮음)
**예상 해결 시간**: 30분

---

#### 4-6. Context & Rules (3개) - **충돌 확률 90%** ⚠️⚠️

**파일 목록**:
```
src/core/context/instructions/user-instructions/external-rules.ts
src/core/context/instructions/user-instructions/rule-helpers.ts
```

**주요 충돌 지점**:

##### external-rules.ts - **충돌 확률 95%** ⚠️⚠️
```typescript
// Cline v3.34.0: .clinerules 개선
export async function loadExternalRules(cwd: string): Promise<ExternalRules> {
  const rulesDir = path.join(cwd, '.clinerules')
  // 새로운 규칙 파싱 로직
  const rules = await parseRulesWithNewFormat(rulesDir)
  return rules
}

// Caret (CARET MODIFICATION): .caretrules 추가
export async function loadExternalRules(cwd: string): Promise<ExternalRules> {
  const clineRulesDir = path.join(cwd, '.clinerules')
  const caretRulesDir = path.join(cwd, '.caretrules')

  const clineRules = await loadRulesFromDir(clineRulesDir)
  const caretRules = await loadRulesFromDir(caretRulesDir)

  return { ...clineRules, ...caretRules }
}

// 충돌: ❌ 같은 함수를 완전히 다르게 수정
```

**수동 해결 난이도**: ⭐⭐⭐⭐⭐ (매우 어려움)
- Cline의 새로운 파싱 로직 이해 필요
- Caret의 이중 디렉토리 지원 유지 필요
- 두 로직을 통합해야 함

**예상 해결 시간**: 2-3시간
**테스트 필요성**: ⭐⭐⭐⭐⭐ (필수)

**위험도**: ⭐⭐⭐⭐⭐ (매우 높음)

---

### 5️⃣ Integrations & Services (12개) - 🟡 높음

**파일 목록**:
```
src/integrations/cli-subagents/subagent_command.ts
src/integrations/claude-code/run.ts
src/integrations/claude-code/types.ts
src/integrations/terminal/TerminalProcess.test.ts
src/integrations/terminal/TerminalRegistry.ts
src/integrations/terminal/TerminalManager.ts
src/integrations/checkpoints/CheckpointGitOperations.ts
src/integrations/checkpoints/CheckpointUtils.ts
src/integrations/notifications/index.ts
src/services/logging/distinctId.ts
src/services/logging/distinctId.test.ts
src/services/mcp/McpHub.ts
src/services/account/CaretAccountService.ts
```

#### 5-1. CLI Subagents (1개) - **충돌 확률 100%** ⚠️⚠️⚠️

##### subagent_command.ts - **신규 파일 충돌**
```typescript
// Cline v3.34.0 (신규 파일)
export async function detectSubagentCommand(): Promise<SubagentInfo | null> {
  // Cline 전용 감지 로직
  const clinePattern = /cline\s+['"]/
  return detectCommand('cline')
}

// Caret (CARET MODIFICATION - 신규 파일)
export async function detectSubagentCommand(): Promise<SubagentInfo | null> {
  // Caret 브랜딩 지원
  const brandName = getBrandName()
  const caretPattern = /(?:caret|cline)\s+['"]/
  return detectCommand(brandName)
}

// 충돌: ❌ 두 브랜치 모두 같은 이름의 신규 파일 생성
```

**충돌 타입**: "Added by both" - 가장 해결하기 어려운 충돌
**수동 해결 필요**: Caret의 브랜딩 로직을 Cline 로직에 통합

**위험도**: ⭐⭐⭐⭐⭐ (매우 높음)
**예상 해결 시간**: 1-2시간

---

#### 5-2. Terminal (3개) - **충돌 확률 60%** 🟠

**파일 목록**:
```
src/integrations/terminal/TerminalManager.ts
src/integrations/terminal/TerminalRegistry.ts
src/integrations/terminal/TerminalProcess.test.ts
```

**TerminalManager.ts 충돌 예상**:
```typescript
// Cline v3.34.0: Subagent 터미널 지원
export class TerminalManager {
  async createTerminal(type: 'normal' | 'subagent') {
    if (type === 'subagent') {
      return new SubagentTerminal()
    }
    return new NormalTerminal()
  }
}

// Caret (CARET MODIFICATION): 브랜딩 + Subagent 감지
export class TerminalManager {
  private isSubagentMode: boolean = false

  async initialize() {
    this.isSubagentMode = await detectSubagentCommand() !== null
    if (this.isSubagentMode) {
      this.setupSubagentTerminal()
    }
  }
}

// 충돌: 같은 클래스를 다르게 확장
```

**위험도**: ⭐⭐⭐⭐☆ (높음)
**예상 해결 시간**: 1시간

---

#### 5-3. 기타 (8개) - **충돌 확률 20%** 🟢

**충돌 낮은 이유**:
- Caret 전용 파일 (CaretAccountService.ts)
- 독립적인 기능 (checkpoints, notifications)
- 로깅만 수정 (distinctId.ts)

**위험도**: ⭐⭐☆☆☆ (낮음)

---

### 6️⃣ Hosts & Standalone (6개) - 🟠 중간

**파일 목록**:
```
src/core/webview/WebviewProvider.ts
src/hosts/vscode/commit-message-generator.ts
src/hosts/vscode/VscodeDiffViewProvider.ts
src/hosts/vscode/VscodeWebviewProvider.ts
src/hosts/host-provider.ts
src/standalone/cline-core.ts
src/standalone/vscode-context.ts
```

#### 6-1. WebviewProvider.ts - **충돌 확률 70%** ⚠️

**예상 충돌**:
```typescript
// Cline v3.34.0: CLI 상태 메시지 추가
export class WebviewProvider {
  async postMessage(message: ExtensionMessage) {
    // CLI 상태 추가
    if (message.type === 'cliStatus') {
      this.sendCliStatus()
    }
  }
}

// Caret (CARET MODIFICATION): Caret 메시지 추가
export class WebviewProvider {
  async postMessage(message: ExtensionMessage) {
    // Caret 상태 추가
    if (message.type === 'caretModeChanged') {
      this.sendCaretMode()
    }
  }
}

// 충돌: 같은 메서드에 다른 로직 추가
```

**위험도**: ⭐⭐⭐⭐☆ (높음)
**예상 해결 시간**: 1시간

---

#### 6-2. Standalone (2개) - **충돌 확률 80%** ⚠️

**cline-core.ts & vscode-context.ts**:
- Cline v3.34.0: CLI 모드 강화
- Caret: CLI 설정 경로 변경 (~/.caret)

**충돌 예상**:
```typescript
// Cline v3.34.0
const CLINE_DIR = process.env.CLINE_DIR || `${os.homedir()}/.cline`

// Caret (CARET MODIFICATION)
const CARET_DIR = process.env.CARET_DIR || `${os.homedir()}/.caret`

// 충돌: 같은 변수명, 다른 값
```

**위험도**: ⭐⭐⭐⭐☆ (높음)
**예상 해결 시간**: 1시간

---

### 7️⃣ Tests (2개) - 🟢 낮음

**파일 목록**:
```
src/test/e2e/diff.test.ts
src/test/host-provider-test-utils.ts
```

**충돌 예측**:
- 테스트 유틸리티 수정 (낮음)
- Mock 데이터 추가 (낮음)

**위험도**: ⭐⭐☆☆☆ (낮음)
**예상 해결 시간**: 30분

---

### 8️⃣ 기타 - extension.ts (1개) - ⚠️⚠️ **매우 높음**

#### src/extension.ts - **충돌 확률 99.9%** ⚠️⚠️⚠️

**최고 위험 파일 중 하나**

**Caret 수정 내역** (12회):
1. Import 추가 (5개)
2. CaretGlobalManager 초기화
3. CaretModeManager 초기화
4. JsonTemplateLoader 초기화
5. Persona 시스템 초기화
6. Context key 변경 (caret.isDevMode)
7. Output channel 이름 변경 (Caret)
8. WebviewProvider 래핑

**Cline v3.34.0 예상 변경**:
1. CLI 감지 로직 추가
2. Subagent 초기화
3. CLI 상태 체크
4. 새로운 명령어 등록

**예상 충돌 시나리오 1: Import 블록**
```typescript
// Cline v3.34.0
import { CliDetector } from '@/services/cli/detector'
import { SubagentManager } from '@/integrations/cli-subagents'

// Caret (CARET MODIFICATION)
import { JsonTemplateLoader } from "@caret/core/prompts/system/JsonTemplateLoader"
import { CaretGlobalManager } from "@caret/managers/CaretGlobalManager"
import { CaretProviderWrapper } from "../caret-src/core/webview/CaretProviderWrapper"

// 충돌: ❌ 같은 Import 블록 위치
```

**예상 충돌 시나리오 2: activate() 함수**
```typescript
// Cline v3.34.0
export async function activate(context: ExtensionContext) {
  // ... 기존 코드

  // CLI 초기화
  const cliDetector = new CliDetector()
  const isCliInstalled = await cliDetector.check()
  if (isCliInstalled) {
    await SubagentManager.initialize()
  }

  Logger.log("Cline extension activated")
}

// Caret (CARET MODIFICATION)
export async function activate(context: ExtensionContext) {
  // ... 기존 코드

  // Caret 초기화
  const initialMode = context.workspaceState.get<"caret" | "cline">("caret.promptSystem.mode", "caret")
  CaretGlobalManager.initialize(initialMode)

  const { CaretModeManager } = await import("@caret/core/modes/CaretModeManager")
  CaretModeManager.setContext(context)

  const sectionsDirPath = vscode.Uri.joinPath(context.extensionUri, "caret-src", "core", "prompts", "sections").fsPath
  await JsonTemplateLoader.getInstance().initialize(sectionsDirPath)

  Logger.log("Caret extension activated")

  try {
    const personaInitializer = new PersonaInitializer(context)
    await personaInitializer.initializePersonaSystem()
  } catch (error) {
    Logger.error("Persona initialization failed", error)
  }
}

// 충돌: ❌ 같은 함수를 완전히 다르게 확장
```

**수동 해결 난이도**: ⭐⭐⭐⭐⭐ (최고)
**예상 해결 시간**: 2-4시간
**영향 범위**: 전체 확장 시작
**테스트 필요성**: ⭐⭐⭐⭐⭐ (필수)

**위험도**: ⭐⭐⭐⭐⭐ (최고)

---

## 📋 우선순위별 해결 전략

### 🔥 Critical (반드시 먼저 해결) - 5개

| 파일 | 충돌 확률 | 난이도 | 예상 시간 | 영향 범위 |
|------|-----------|--------|-----------|-----------|
| **src/extension.ts** | 99.9% | ⭐⭐⭐⭐⭐ | 2-4시간 | 전체 시스템 |
| **src/core/task/index.ts** | 99% | ⭐⭐⭐⭐⭐ | 3-4시간 | 태스크 실행 |
| **src/core/controller/index.ts** | 95% | ⭐⭐⭐⭐⭐ | 2-3시간 | 메시지 라우팅 |
| **src/core/context/.../external-rules.ts** | 95% | ⭐⭐⭐⭐⭐ | 2-3시간 | 규칙 시스템 |
| **src/integrations/cli-subagents/subagent_command.ts** | 100% | ⭐⭐⭐⭐⭐ | 1-2시간 | CLI Subagent |

**소계**: 10-16시간

---

### ⚠️ High (다음으로 해결) - 8개

| 파일 | 충돌 확률 | 난이도 | 예상 시간 |
|------|-----------|--------|-----------|
| **src/shared/ExtensionMessage.ts** | 90% | ⭐⭐⭐⭐☆ | 30분-1시간 |
| **src/shared/storage/types.ts** | 70% | ⭐⭐⭐⭐☆ | 20분 |
| **src/core/prompts/system-prompt/components/cli_subagents.ts** | 100% | ⭐⭐⭐☆☆ | 30분 |
| **src/core/prompts/system-prompt/types.ts** | 70% | ⭐⭐⭐☆☆ | 30분 |
| **src/core/storage/state-keys.ts** | 80% | ⭐⭐⭐☆☆ | 1시간 |
| **src/core/webview/WebviewProvider.ts** | 70% | ⭐⭐⭐⭐☆ | 1시간 |
| **src/integrations/terminal/TerminalManager.ts** | 60% | ⭐⭐⭐⭐☆ | 1시간 |
| **src/standalone/cline-core.ts** | 80% | ⭐⭐⭐⭐☆ | 1시간 |

**소계**: 5.5-7시간

---

### 🟡 Medium (여유있게 해결) - 15개

**충돌 확률**: 40-60%
**예상 시간**: 3-5시간
**난이도**: ⭐⭐⭐☆☆

파일: Controller 나머지, Storage 나머지, Prompts 나머지 등

---

### 🟢 Low (자동 해결 가능) - 70개

**충돌 확률**: 0-20%
**자동 해결**: 대부분 가능
**수동 확인**: 30분-1시간

파일: Proto 자동생성, Tool handlers, Tests, 로깅만 수정된 파일

---

## 🎯 자동 머징 시뮬레이션

### 시나리오: `git merge origin/cline-v3.34.0` 실행

```bash
# 1. 머징 시도
git merge origin/cline-v3.34.0

# 예상 출력
Auto-merging src/generated/nice-grpc/cline/file.ts
Auto-merging src/generated/nice-grpc/cline/browser.ts
... (14개 proto 파일 자동 성공)

Auto-merging src/core/api/providers/cline.ts
Auto-merging src/core/api/providers/dify.ts
... (일부 provider 자동 성공)

CONFLICT (content): Merge conflict in src/extension.ts
CONFLICT (content): Merge conflict in src/core/task/index.ts
CONFLICT (content): Merge conflict in src/core/controller/index.ts
CONFLICT (content): Merge conflict in src/shared/ExtensionMessage.ts
CONFLICT (content): Merge conflict in src/core/context/instructions/user-instructions/external-rules.ts
CONFLICT (add/add): Merge conflict in src/integrations/cli-subagents/subagent_command.ts
CONFLICT (content): Merge conflict in src/core/storage/state-keys.ts
CONFLICT (content): Merge conflict in src/core/prompts/system-prompt/components/cli_subagents.ts
CONFLICT (content): Merge conflict in src/core/prompts/system-prompt/types.ts
CONFLICT (content): Merge conflict in src/shared/storage/types.ts
... (추가 충돌 20-30개)

Automatic merge failed; fix conflicts and then commit the result.
```

### 예상 충돌 파일 수: **30-40개** (98개 중)

**자동 성공**: 58-68개 (59-69%)
**수동 해결**: 30-40개 (31-41%)

---

## ⏱️ 예상 작업 시간표

### 최적 시나리오 (경험 많은 개발자)
```
Critical 파일 (5개): 10시간
High 파일 (8개): 5.5시간
Medium 파일 (15개): 3시간
Low 파일 검증: 1시간
테스트 & 디버깅: 2시간
--------------------
총 예상 시간: 21.5시간 (약 3일)
```

### 보통 시나리오
```
Critical 파일 (5개): 16시간
High 파일 (8개): 7시간
Medium 파일 (15개): 5시간
Low 파일 검증: 2시간
테스트 & 디버깅: 4시간
--------------------
총 예상 시간: 34시간 (약 4-5일)
```

### 최악 시나리오 (예상치 못한 충돌)
```
모든 수동 해결: 25시간
추가 디버깅: 10시간
통합 테스트 실패 수정: 10시간
--------------------
총 예상 시간: 45시간 (약 1주일)
```

---

## 🛡️ 리스크 요인

### 1. 알려진 위험 (높음)
- **CLI Subagent 시스템**: Cline v3.34.0의 핵심 기능과 Caret 구현의 충돌
- **규칙 시스템 변경**: `.clinerules` 파싱 로직 대폭 수정 가능성
- **타입 정의 충돌**: ExtensionMessage, SystemPromptArgs 등

### 2. 미지의 위험 (중간)
- Cline v3.34.0의 리팩토링 범위 불명
- 테스트 실패 가능성
- 숨겨진 의존성 변경

### 3. 브레이킹 체인지 (낮음)
- Proto 스키마 호환성 깨짐 (낮음 - 버전 관리됨)
- Public API 변경 (낮음 - 안정화된 버전)

---

## ✅ 권장 전략

### 옵션 1: 단계별 머징 (권장) ⭐⭐⭐⭐⭐
```bash
# 1. 안전한 파일부터 머징 (Proto, Tests)
git checkout caret-main
git merge --no-commit --no-ff cline-v3.34.0
git checkout --theirs src/generated/  # Proto 자동생성 우선
npm run protos  # 재생성

# 2. Critical 파일만 수동 해결
git checkout --conflict=merge src/extension.ts
git checkout --conflict=merge src/core/task/index.ts
# ... 5개 Critical 파일 수동 해결

# 3. 테스트 실행하며 단계별 커밋
git add src/extension.ts
git commit -m "fix: Resolve extension.ts merge conflicts"
npm run test:backend
# 성공 시 다음 파일

# 4. 반복
```

**예상 시간**: 3-4일
**성공률**: 95%
**위험도**: 낮음

---

### 옵션 2: 선택적 체리픽 (안전) ⭐⭐⭐⭐☆
```bash
# Cline v3.34.0의 변경사항을 선별적으로 가져오기
git log cline-v3.34.0..cline-v3.32.7 --oneline

# CLI Subagent 관련 커밋만 선택
git cherry-pick <commit-hash>  # Subagent detection
git cherry-pick <commit-hash>  # Subagent execution
# ... 필요한 커밋만

# Caret 수정 적용
# 수동으로 CARET MODIFICATION 추가
```

**예상 시간**: 5-7일
**성공률**: 99%
**위험도**: 매우 낮음

---

### 옵션 3: 전체 자동 머징 후 수정 (비권장) ⭐⭐☆☆☆
```bash
# 한번에 머징 시도
git merge cline-v3.34.0

# 30-40개 충돌 파일 동시 해결
# ... 높은 실수 확률
```

**예상 시간**: 5-10일 (디버깅 포함)
**성공률**: 60%
**위험도**: 높음
**문제**: 테스트 실패 원인 찾기 어려움

---

## 📊 최종 예측 요약

| 항목 | 값 |
|------|-----|
| **총 파일** | 98개 |
| **자동 성공 예상** | 58-68개 (59-69%) |
| **수동 해결 필요** | 30-40개 (31-41%) |
| **Critical 충돌** | 5개 |
| **High 충돌** | 8개 |
| **예상 작업 시간** | 21.5-45시간 (3-7일) |
| **권장 전략** | 단계별 머징 |
| **성공 가능성** | 95% (단계별 머징 시) |

---

## 🚨 주의사항

### 머징 전 필수 체크리스트
- [ ] Cline v3.34.0 변경사항 상세 확인 (CHANGELOG, PR)
- [ ] 테스트 환경 준비 (별도 브랜치)
- [ ] 백업 생성 (현재 caret-main)
- [ ] 시간 확보 (최소 3-4일)
- [ ] 팀원 리뷰 가능 여부

### 머징 중 필수 사항
- [ ] 단계별 커밋 (파일별/카테고리별)
- [ ] 각 단계마다 테스트 실행
- [ ] CARET MODIFICATION 주석 유지
- [ ] 변경 로그 작성

### 머징 후 필수 검증
- [ ] `npm run compile` 성공
- [ ] `npm run test:backend` 통과
- [ ] `npm run test:webview` 통과 (프론트엔드 나중에)
- [ ] VS Code에서 확장 로드 테스트
- [ ] 주요 기능 수동 테스트 (Task 실행, CLI Subagent 등)

---

*생성일: 2025-11-03*
*예측 기반: 지금까지 분석한 98개 파일의 CARET MODIFICATION 패턴*
*정확도: 80-85% (실제 Cline v3.34.0 변경사항 확인 후 조정 필요)*
