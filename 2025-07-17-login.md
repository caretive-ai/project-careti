# Caret 로그인 및 사용자 정보 업데이트 흐름 분석 (2025-07-17)

## 1. 개요

이 문서는 `webview-ui/src/caret/components/CaretAccountInfoCard.tsx` 컴포넌트의 `user` 변수가 어떻게 결정되고 업데이트되는지에 대한 심층 분석을 제공합니다. 이 분석은 향후 로그인 관련 기능 수정 및 디버깅에 필요한 정보를 담고 있습니다.

`user` 변수는 다음 코드 라인에서 정의됩니다.

```typescript
let user = apiConfiguration?.clineApiKey ? firebaseUser || userInfo : undefined
```

이 값은 `firebaseUser`, `userInfo`, `apiConfiguration` 세 가지 핵심 상태에 의존하며, 이 중 하나라도 변경되면 `user` 값이 재계산되고 UI가 업데이트됩니다.

## 2. 핵심 변수 상세

-   **`firebaseUser`**: Firebase 인증을 통해 얻는 사용자 객체입니다.
    -   **출처**: `webview-ui/src/context/FirebaseAuthContext.tsx`의 `useFirebaseAuth()` 훅.
    -   **역할**: 사용자의 Firebase 인증 상태(로그인/로그아웃)를 직접적으로 나타냅니다.
-   **`userInfo`**: Caret 백엔드에서 관리하는 사용자 정보 객체입니다.
    -   **출처**: `webview-ui/src/context/ExtensionStateContext.tsx`의 `useExtensionState()` 훅.
    -   **역할**: Firebase 인증 정보 외에 Caret 서비스 관련 사용자 정보(예: 플랜, 결제 상태 등)를 포함합니다.
-   **`apiConfiguration`**: API 키 등 Caret 서비스 연동에 필요한 설정 객체입니다.
    -   **출처**: `webview-ui/src/context/ExtensionStateContext.tsx`의 `useExtensionState()` 훅.
    -   **역할**: `clineApiKey` (Caret API 키)의 존재 여부를 통해 로그인 상태를 판별하는 주요 조건으로 사용됩니다.

## 3. 전체 로그인 흐름 (End-to-End)

사용자가 로그인 버튼을 클릭하는 시점부터 UI가 업데이트되기까지의 과정은 다음과 같습니다.

### 1단계: UI - 로그인 시작

-   **파일**: `webview-ui/src/caret/components/CaretAccountInfoCard.tsx`
-   **동작**: 사용자가 "Sign up with Caret" 버튼을 클릭하면 `handleLogin` 함수가 실행됩니다.
-   **구현**: `AccountServiceClient.accountLoginClicked()`를 호출하여 백엔드에 로그인 프로세스 시작을 요청합니다.

### 2단계: 백엔드 - 인증 URL 생성

-   **파일**: `src/core/controller/account/accountLoginClicked.ts`
-   **동작**: `accountLoginClicked` 컨트롤러 메서드가 gRPC 요청을 처리합니다.
-   **구현**:
    1.  CSRF 공격 방지를 위한 `nonce`를 생성하여 `secretStorage`에 저장합니다.
    2.  인증 서버 URL에 `nonce`와 콜백 URL(`vscode://caretive.caret/auth`)을 포함하여 최종 인증 URL을 생성합니다.
    3.  `vscode.env.openExternal()`을 통해 시스템의 기본 브라우저에서 인증 URL을 엽니다.

### 3단계: 확장 프로그램 - 인증 콜백 처리

-   **파일**: `src/extension.ts`
-   **동작**: 사용자가 외부 브라우저에서 인증을 완료하면, VSCode는 `vscode://caretive.caret/auth` URI를 통해 다시 활성화됩니다.
-   **구현**:
    1.  `activate` 함수에 등록된 `vscode.window.registerUriHandler`가 해당 URI를 가로챕니다.
    2.  `handleUri` 함수는 `/auth` 경로를 확인하고, 쿼리 파라미터에서 `token` (Firebase Custom Token), `state` (`nonce`), `apiKey` (Caret API Key)를 추출합니다.
    3.  `controller.validateAuthState(state)`를 호출하여 2단계에서 저장한 `nonce`와 일치하는지 검증합니다.
    4.  검증이 성공하면 `controller.handleAuthCallback(token, apiKey)`를 호출합니다.

### 4단계: 컨트롤러 - 인증 정보 저장 및 전파

-   **파일**: `src/core/controller/index.ts`
-   **동작**: `handleAuthCallback` 메서드가 최종 인증 처리를 수행합니다.
-   **구현**:
    1.  `storeSecret`을 사용하여 전달받은 `apiKey`를 `secretStorage`에 `caretApiKey`라는 키로 저장합니다.
    2.  `sendAuthCallbackEvent(customToken)`를 호출하여 웹뷰(프론트엔드)로 Firebase 커스텀 토큰을 전송합니다.
    3.  `updateWorkspaceState`를 통해 `apiProvider`를 `"caret"`으로 설정합니다.
    4.  `postStateToWebview()`를 호출하여 변경된 `apiConfiguration`을 포함한 전체 확장 프로그램 상태를 웹뷰로 브로드캐스트합니다.

### 5단계: 프론트엔드 - Firebase 로그인

-   **파일**: `webview-ui/src/context/FirebaseAuthContext.tsx`
-   **동작**: 백엔드로부터 받은 커스텀 토큰을 사용하여 Firebase에 로그인합니다.
-   **구현**:
    1.  `useEffect` 훅에서 `AccountServiceClient.subscribeToAuthCallback`을 구독하고 있습니다.
    2.  4단계에서 보낸 커스텀 토큰을 수신하면 `signInWithToken` 함수가 호출됩니다.
    3.  `signInWithCustomToken(auth, token)`을 실행하여 Firebase에 로그인합니다.
    4.  로그인이 성공하면 `onAuthStateChanged` 리스너가 트리거되어 `user` 상태(`firebaseUser`)가 새로운 Firebase 사용자 객체로 업데이트됩니다.

### 6단계: 프론트엔드 - 상태 동기화 및 UI 업데이트

-   **파일**: `webview-ui/src/context/ExtensionStateContext.tsx`
-   **동작**: 백엔드로부터 받은 최신 상태를 반영하고, `FirebaseAuthContext`의 변경 사항과 결합하여 최종 UI를 렌더링합니다.
-   **구현**:
    1.  `useEffect` 훅에서 `StateServiceClient.subscribeToState`를 구독하고 있습니다.
    2.  4단계에서 `postStateToWebview()`가 호출되면, 이 구독을 통해 최신 `ExtensionState` JSON을 수신합니다.
    3.  수신된 데이터로 `apiConfiguration`과 `userInfo`를 포함한 내부 상태를 업데이트합니다.
    4.  `CaretAccountInfoCard` 컴포넌트는 `useFirebaseAuth`와 `useExtensionState` 훅을 통해 업데이트된 `firebaseUser`, `userInfo`, `apiConfiguration` 값을 받습니다.
    5.  `user` 변수가 재계산되고, `user`가 존재하므로 "View Billing & Usage" 버튼이 표시되는 UI로 리렌더링됩니다.

## 4. 다음 작업을 위한 제언

-   **분산된 로직**: 로그인 흐름은 UI, 컨트롤러, 확장 프로그램 진입점, 다수의 Context Provider 등 여러 파일에 걸쳐 분산되어 있어 추적이 복잡합니다. 관련 로직을 수정할 때는 이 문서에 명시된 모든 파일을 함께 고려해야 합니다.
-   **상태 동기화**: `user` 값은 Firebase 상태(`firebaseUser`)와 백엔드 상태(`userInfo`, `apiConfiguration`) 양쪽에 의존합니다. 두 상태 간의 동기화가 어긋날 경우 예기치 않은 UI 동작이 발생할 수 있으므로, 상태 업데이트는 항상 `postStateToWebview`와 같은 단일 소스를 통해 전파하는 현재 구조를 유지하는 것이 중요합니다.
-   **에러 처리**: 각 단계(네트워크 요청, URI 처리, 상태 검증 등)에서 발생할 수 있는 오류에 대한 처리가 중요합니다. 특히 `handleAuthCallback`에서 오류 발생 시 API 키나 토큰이 저장되지 않도록 방어 로직을 강화할 수 있습니다.

## 5. AI 어시스턴트 분석 요약 (2025-07-17)

Caret AI 어시스턴트가 `authUrl` 로그를 기반으로 로그인 흐름을 분석한 결과, 위 문서에 기술된 내용과 일치함을 확인했습니다. 주요 흐름은 다음과 같이 요약할 수 있습니다.

1.  **로그인 시작**: 사용자가 UI에서 로그인 버튼을 클릭하면, 백엔드 컨트롤러(`accountLoginClicked`)가 호출되어 인증용 `authUrl`을 생성하고 외부 브라우저를 엽니다.
2.  **인증 및 콜백**: 사용자가 브라우저에서 성공적으로 인증을 완료하면, `vscode://caretive.caret/auth` 형태의 커스텀 URI로 리디렉션됩니다.
3.  **콜백 처리**: VSCode 확장 프로그램의 메인 진입점(`src/extension.ts`)에 등록된 `URI Handler`가 이 콜백을 수신합니다.
4.  **정보 처리 및 저장**: `URI Handler`는 URI에서 토큰과 API 키를 추출하고, 컨트롤러의 `handleAuthCallback` 함수를 호출하여 관련 정보를 안전하게 저장합니다.
5.  **상태 전파 및 UI 업데이트**: 컨트롤러는 상태 변경 이벤트를 프론트엔드(웹뷰)로 전송합니다. 프론트엔드에서는 `Context`를 통해 이 상태 변화를 감지하고, `CaretAccountInfoCard` 컴포넌트의 UI를 자동으로 업데이트하여 로그인 완료 상태를 반영합니다.

이 분석을 통해 `authUrl`이 직접 UI 컴포넌트에 값을 전달하는 것이 아니라, 전체 인증 플로우를 거쳐 최종적으로 상태 동기화를 통해 UI가 업데이트되는 간접적인 구조임을 재확인했습니다.
