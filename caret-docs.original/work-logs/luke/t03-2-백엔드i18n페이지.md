# Luke Yang - t03-2 백엔드 메시지 i18n, 웰컴페이지 이식 (2단계)

**작업 기간**: 2025-08-31 ~ 진행중  
**담당자**: Luke Yang  
**우선순위**: Medium  
**AI 어시스턴트**: Claude Code

## ⚠️ **사전 작업 필수 (2025-08-31)**

## 🎯 2단계 목표 (수정됨): 백엔드 메시지 i18n 웹뷰 적용

### 2.1. 📋 백엔드 메시지 i18n 웹뷰 통합
- ✅ **기본 시스템 완료**: backend-message-filter.ts (22/22 테스트 통과)
- 🔄 **웹뷰 통합**: 실제 ClineMessage 렌더링에 필터 적용
- 🔄 **모드 연동**: CaretProvider와 브랜드 모드 시스템 연결

### 2.2. 📋 수동 i18n 적용 방식 (변경됨)
- **방향 변경**: 자동 브랜드 전환 → 수동 i18n 적용으로 단순화
- i18n 파일들의 하드코딩 텍스트는 필요시 수동으로 처리
- 복잡한 자동화 시스템 제거하고 핵심 기능에 집중

## 📋 기반 시스템 (1단계에서 완성됨)

### ✅ i18n 토글 시스템 파일들
1. **Frontend**: `/webview-ui/src/caret/i18n/brand-toggle.ts`
   - `getCurrentBrandMode()`, `isI18nEnabled()` 함수
   - 브랜드별 i18n 활성화/비활성화 제어
   - Cline 모드: i18n 비활성화, Caret 모드: i18n 활성화

2. **Backend Filter**: `/webview-ui/src/caret/i18n/backend-message-filter.ts`
   - 웹뷰에서 백엔드 메시지 i18n 필터링 처리
   - TDD 완성 (22/22 테스트 통과)
   - 브랜드별 메시지 번역 시스템

3. **Brand Converter**: `/caret-b2b/tools/brand-converter.js` (확장 예정)
   - i18n 파일 하드코딩 텍스트 자동 브랜드 전환 기능 추가
   - 브랜드별 자동 변환 설정

## 📋 2단계 세부 구현 계획 (단순화됨)

### 🔧 작업 1: 웹뷰 백엔드 메시지 필터 통합

#### **목표**: 기존 TDD 완성된 필터를 실제 웹뷰 ClineMessage 렌더링에 적용

#### **작업 내용**
```bash
# i18n 적용 파일에서 하드코딩된 브랜드명 검색
grep -r "Cline" webview-ui/src/ --include="*.tsx" --include="*.ts" -n
grep -r "cline" webview-ui/src/ --include="*.tsx" --include="*.ts" -n
grep -r "CLINE" webview-ui/src/ --include="*.tsx" --include="*.ts" -n

# i18n 키와 하드코딩 텍스트가 혼재된 파일들 식별
find webview-ui/src/ -name "*.tsx" -o -name "*.ts" | xargs grep -l "useTranslation\|t("
```

#### **브랜드 전환 스크립트 확장**
```javascript
// brand-converter.js에 i18n 파일 처리 기능 추가
const I18N_BRAND_MAPPINGS = {
  // 하드코딩 텍스트 → 브랜드별 변환
  hardcoded_texts: {
    "Cline": "{brand.appName}",
    "cline": "{brand.appNameLower}", 
    "CLINE": "{brand.appNameUpper}"
  },
  // i18n 키는 그대로 유지
  preserve_i18n_keys: true
}
```

#### **품질 검증 도구 활용**
```bash
# 브랜드 설정 유효성 검사
node caret-b2b/tools/brand-config-validator.js

# i18n 매핑 추가 후 자동 검증 및 수정
node caret-b2b/tools/brand-config-validator.js --fix
```

#### **결과물**
- i18n 시스템도 포함된 통합 브랜드 전환
- 하드코딩 텍스트만 변환, i18n 키는 보존
- cline ↔ caret ↔ codecenter 전체 자동화

#### **완료 기준**: i18n 적용 페이지의 모든 하드코딩 브랜드명 자동 전환 동작

---

### 🔧 작업 2: 웹뷰 i18n 필터 시스템 통합

#### **목표**: 기존 TDD 완성된 필터를 실제 웹뷰 ClineMessage 렌더링에 적용

#### **구현 위치**: `/webview-ui/src/components/` 내 ClineMessage 관련 컴포넌트

#### **작업 내용**
```typescript
// 1단계: ClineMessage 렌더링 위치 찾기
// 2단계: processBackendMessage() 함수 적용
import { processBackendMessage } from '@/caret/i18n/backend-message-filter'

const displayMessage = processBackendMessage(clineMessage.text, {
  mode: getCurrentBrandMode(),
  enableBackendI18n: isBackendI18nEnabled(),
  language: 'ko'
})
```

#### **결과물**
- 웹뷰에서 실제 백엔드 메시지 번역 동작 확인
- Cline/Caret 모드별 동작 검증
- 통합 테스트 작성

#### **완료 기준**: 웹뷰에서 "Cline wants to edit" → "Caret이 파일을 편집하려고 합니다" 번역 확인

---

### 🔧 작업 3: caret-main 모드 설정 UI 연동 조사

#### **목표**: caret-main의 Cline/Caret 모드 토글 UI를 찾아서 웹뷰 이식 준비

#### **작업 내용**  
```bash
# caret-main에서 모드 설정 관련 파일 검색
find caret-main -name "*.tsx" -o -name "*.ts" | xargs grep -l "cline.*mode\|caret.*mode"
find caret-main -name "*.tsx" -o -name "*.ts" | xargs grep -l "modeSystem\|system.*mode"
```

#### **분석 대상**
- 설정 UI 컴포넌트 구조
- 모드 설정 저장/로드 방식  
- VS Code 설정과의 연동 방법
- UI/UX 패턴

#### **결과물**
- `mode-settings-analysis.md`: 모드 설정 시스템 분석 보고서
- 웹뷰 이식 계획서 (컴포넌트, 상태관리, 설정 동기화)

#### **완료 기준**: caret-main의 모드 설정 시스템 완전 이해 및 이식 방안 정리

---

### 🔧 작업 4: CodeCenter 브랜딩 백엔드 메시지 확장

#### **목표**: codecenter 브랜드용 백엔드 메시지 i18n 설정 추가

#### **작업 내용**
```json
// backend-message-mappings.json 확장
{
  "brandSpecific": {
    "codecenter": {
      "wants_to_edit": "CodeCenter가 파일을 편집합니다",
      "task_completed": "CodeCenter 작업이 완료되었습니다"
    }
  }
}
```

#### **구현 범위**
- codecenter 전용 메시지 번역 추가
- brand-toggle.ts에 codecenter 설정 추가
- codecenterbot vs caretbot 차별화 요소 정의

#### **결과물**
- codecenter 브랜드 메시지 완전 지원
- 3-브랜드 (cline/caret/codecenter) 통합 테스트

#### **완료 기준**: codecenter 모드에서 브랜드별 백엔드 메시지 번역 동작

---

### 🔧 작업 5: 옵션 토글 UI 구현

#### **목표**: 사용자가 백엔드 메시지 i18n을 켜고 끌 수 있는 설정 UI

#### **구현 위치**: 웹뷰 설정 페이지 또는 모드 설정과 통합

#### **작업 내용**
```tsx
// 설정 컴포넌트 예시
<ToggleSwitch
  label="백엔드 메시지 번역"
  checked={isBackendI18nEnabled()}
  onChange={toggleBackendI18n}
  description="Caret 모드에서 백엔드 메시지를 한국어로 번역"
/>
```

#### **기능 요구사항**
- 실시간 토글 (새로고침 없이 즉시 적용)
- 설정 상태 저장 (VS Code 설정 또는 local storage)
- 브랜드 모드와 연동 (Cline 모드에서는 비활성화)

#### **완료 기준**: 설정 UI에서 토글 시 즉시 백엔드 메시지 번역 on/off 동작

---

## 🎯 작업 우선순위 및 진행 순서

### **1단계 (필수)**: 작업 1 → 작업 2
i18n 자동 브랜드 전환 구현 후 웹뷰 통합으로 기본 기능 동작

### **2단계 (중요)**: 작업 3 → 작업 5  
모드 설정 시스템 분석 후 UI 구현으로 사용자 경험 완성

### **3단계 (확장)**: 작업 4
B2B 브랜딩 지원으로 시스템 완성

### **병렬 가능**: 작업 1과 작업 3 동시 진행 가능

## 🚨 각 작업별 주의사항

### **작업 1**: i18n 자동 전환 구현
- i18n 키는 보존하고 하드코딩 텍스트만 브랜드별 변환
- `brand-converter.js`에 i18n 파일 처리 로직 추가
- 백엔드는 수정하지 않고 웹뷰에서만 필터링 처리

### **작업 3**: caret-main 참조용
- caret-main은 참고용으로만 사용, 직접 수정하지 말 것
- 필요한 컴포넌트는 별도 구현 또는 복사해서 수정

### **작업 4-5**: 브랜드 호환성
- Cline 모드에서는 모든 i18n 기능 비활성화 유지
- codecenter 브랜딩이 caret 기본 기능에 영향주지 않도록 주의

### **전체 작업**: 품질 관리 및 TDD 원칙 준수
- 모든 새 기능은 테스트 먼저 작성
- 기존 22/22 테스트 통과 상태 유지하면서 확장
- brand-config-validator.js로 설정 파일 유효성 지속 검증

## 🚨 중요 원칙

### Luke 지시사항 (업데이트됨)
- **"i18n도 자동 전환 포함"** - 하드코딩 텍스트만 브랜딩 스크립트로 자동화
- **"주석 잘 달기"** - 모든 수정에 한글 주석 필수  
- **단계별 허락** - 각 단계마다 확인 후 진행

### 기술적 제약사항
- 업스트림 머징 고려 - 최소한의 Cline 파일 수정
- CARET MODIFICATION 주석 표준 준수
- Level 1 독립 모듈 우선 활용

## 📋 현재 상태 (2025-08-31 업데이트)
- **1단계**: ✅ **100% 완료** (Luke 테스트 완료)
  - 1.1 cline ↔ caret: ✅ 완료 
  - 1.2 caret → codecenter: ✅ TDD 완료 (5/5 테스트 통과)
  - 1.3 이미지 리소스 전환: ✅ TDD 완료 (5/5 테스트 통과)
- **2단계**: 🔄 **시작됨** (백엔드 메시지 i18n 조사 중)

## 📋 2.1 백엔드 메시지 i18n 적용 - 현재 작업

### 🔍 웹뷰 백엔드 메시지 조사 결과
**목표**: 웹뷰로 전송되는 ClineMessage의 text 필드 중 하드코딩된 메시지를 i18n으로 번역

**발견된 메시지 유형**:
1. **시스템 알림**: `showSystemNotification` (OS 네이티브 - 제외)
2. **VS Code 메시지**: `HostProvider.window.showMessage` (VS Code UI - 제외)  
3. **웹뷰 메시지**: `ClineMessage.text` (웹뷰 표시 - ⭐ 타겟)

### 🎯 새로운 방향 - i18n 자동 브랜드 전환 (2025-08-31 업데이트)

**기존 방향**: 수동 주석 관리 방식
- i18n 파일의 하드코딩 텍스트 수동 관리
- 브랜드 전환 시 주석으로 수동 처리

**새로운 방향**: 완전 자동 브랜드 전환
1. ✅ **i18n 키 보존**: useTranslation, t() 등 i18n 시스템은 그대로 유지
2. ⭐ **하드코딩 텍스트 자동 변환**: "Cline", "cline", "CLINE" 등을 브랜드별로 자동 변환
3. 🚀 **브랜드 전환 스크립트 확장**: brand-converter.js에 i18n 파일 처리 기능 추가

**새 구현 방향**:
```javascript
// brand-converter.js 확장
node caret-b2b/tools/brand-converter.js caret forward --include-i18n
// i18n 파일의 하드코딩 텍스트도 자동 브랜드 전환
```

### 📋 새로운 구현 계획 (자동 전환 방식)

#### **Phase 1**: i18n 자동 브랜드 전환 시스템 구현 ⭐
- brand-converter.js에 i18n 파일 처리 기능 추가
- 하드코딩 텍스트만 자동 변환, i18n 키는 보존
- cline ↔ caret ↔ codecenter 전체 자동화

#### **Phase 2**: 웹뷰 백엔드 메시지 필터 통합 ✅
- 기존 TDD 완성된 필터를 실제 ClineMessage 렌더링에 적용  
- 웹뷰에서 백엔드 메시지 i18n 번역 처리
- 브랜드 모드별 동작 (Cline: 원본, Caret: 번역)

#### **Phase 3**: 통합 테스트 및 UI 연동
- i18n 자동 전환 + 백엔드 메시지 필터 통합 동작 확인
- 모드 설정 UI 이식 (caret-main 참고)
- E2E 테스트로 전체 시스템 검증

### 🎯 옵션 기능 구현 사양

#### **연동 방식**: caret-main 기존 모드 설정 활용
- **기존 시스템**: caret-main 일반 설정의 caret/cline 모드 설정
- **원래 목적**: 시스템 프롬프트 설정 (아직 미이식)
- **현재 활용**: 백엔드 메시지 i18n 토글로도 사용

#### **동작 사양**
- **Cline 모드 켜면**: 백엔드 메시지 i18n **OFF** (영어 원본)
- **Caret 모드 켜면**: 백엔드 메시지 i18n **ON** (한국어 번역)

#### **구현 위치**
- **설정 UI**: caret-main 일반 설정의 기존 caret/cline 모드 토글 활용
- **웹뷰 이식**: 해당 설정 컴포넌트를 웹뷰로 이식 시 포함
- **우선순위**: 모드 설정 기능만 먼저 포함 (시스템 프롬프트는 나중)

### 🧹 작업 진행사항 (새 방향)

#### ✅ 완료된 작업
- 백엔드 메시지 i18n 필터 TDD 구현 완료 (22/22 테스트 통과)
- 웹뷰 필터링 시스템 단위 구현 완성
- 새로운 방향 결정: i18n 자동 브랜드 전환 방식 채택

#### 🔄 다음 단계 (새 우선순위)
- **최우선**: brand-converter.js에 i18n 파일 처리 기능 추가
- **품질 검증**: brand-config-validator.js로 브랜드 설정 유효성 확인
- Phase 2: 웹뷰 ClineMessage 렌더링에 백엔드 필터 적용
- Phase 3: i18n 자동 전환 + 백엔드 필터 통합 테스트
- **추가**: caret-main 모드 설정 UI 이식

#### 📋 i18n 자동 전환 구현 방향
- 하드코딩 텍스트 ("Cline", "cline", "CLINE") 자동 브랜드 변환
- i18n 키 (useTranslation, t()) 보존
- `--include-i18n` 플래그로 i18n 파일 포함 전환

## 🧪 TDD 테스트 계획 (2025-08-31)

### 📋 테스트 전략

#### **RED-GREEN-REFACTOR 사이클**
1. **RED**: 실패하는 테스트 작성
2. **GREEN**: 최소한의 구현으로 통과
3. **REFACTOR**: 코드 품질 개선

#### **테스트 범위**
- ✅ 백엔드 메시지 패턴 매칭
- ✅ i18n 키 매핑 정확성  
- ✅ 브랜드 모드별 동작
- ✅ 옵션 토글 동작
- ✅ 에러 케이스 처리

### 📋 테스트 케이스 설계

#### **1. 백엔드 메시지 패턴 매칭 테스트**
```javascript
describe('Backend Message Pattern Matching', () => {
  test('should match "Cline wants to edit" pattern', () => {
    const message = "Cline wants to edit package.json"
    expect(isBackendMessage(message)).toBe(true)
    expect(getMessageType(message)).toBe('wants_to_edit')
  })
  
  test('should match "Task Completed" pattern', () => {
    const message = "Task Completed"
    expect(isBackendMessage(message)).toBe(true)
    expect(getMessageType(message)).toBe('task_completed')
  })
  
  test('should not match regular user messages', () => {
    const message = "Hello, how are you?"
    expect(isBackendMessage(message)).toBe(false)
  })
})
```

#### **2. i18n 키 매핑 테스트**
```javascript
describe('i18n Key Mapping', () => {
  test('should map "wants_to_edit" to Korean', () => {
    const result = translateBackendMessage('wants_to_edit', 'ko')
    expect(result).toBe('파일을 편집하려고 합니다')
  })
  
  test('should handle brand name replacement', () => {
    const result = translateBackendMessage('wants_to_edit', 'ko', { brandName: 'Caret' })
    expect(result).toContain('Caret')
  })
})
```

#### **3. 브랜드 모드별 동작 테스트**
```javascript
describe('Brand Mode Behavior', () => {
  test('should return original message in Cline mode', () => {
    const message = "Cline wants to edit file.js"
    const result = processBackendMessage(message, { mode: 'cline' })
    expect(result).toBe(message)
  })
  
  test('should translate message in Caret mode', () => {
    const message = "Cline wants to edit file.js"  
    const result = processBackendMessage(message, { mode: 'caret' })
    expect(result).toContain('파일을 편집하려고')
    expect(result).toContain('Caret')
  })
})
```

#### **4. 옵션 토글 테스트**
```javascript
describe('Option Toggle', () => {
  test('should disable i18n when toggle is off', () => {
    const message = "Cline wants to create new file"
    const result = processBackendMessage(message, { 
      mode: 'caret', 
      enableBackendI18n: false 
    })
    expect(result).toBe(message) // 원본 그대로
  })
  
  test('should enable i18n when toggle is on', () => {
    const message = "Cline wants to create new file"
    const result = processBackendMessage(message, { 
      mode: 'caret', 
      enableBackendI18n: true 
    })
    expect(result).not.toBe(message) // 번역됨
  })
})
```

#### **5. 에러 케이스 테스트**
```javascript
describe('Error Cases', () => {
  test('should handle unknown message types gracefully', () => {
    const message = "Unknown backend message format"
    expect(() => processBackendMessage(message)).not.toThrow()
  })
  
  test('should handle missing i18n keys', () => {
    const result = translateBackendMessage('nonexistent_key', 'ko')
    expect(result).toBe('nonexistent_key') // fallback
  })
})
```

### 📋 구현 파일 구조

#### **테스트 파일**
- `/webview-ui/src/caret/i18n/__tests__/backend-message-filter.test.ts`

#### **구현 파일**  
- `/webview-ui/src/caret/i18n/backend-message-filter.ts`
- `/webview-ui/src/caret/i18n/backend-message-mappings.json`

### 📋 테스트 실행 계획

#### **Phase 1**: 기본 패턴 매칭 (RED → GREEN)
- 백엔드 메시지 식별 함수
- 기본적인 패턴 매칭 로직

#### **Phase 2**: i18n 매핑 (RED → GREEN)  
- 메시지 타입별 번역 키 매핑
- 한국어 번역 데이터

#### **Phase 3**: 브랜드 모드 연동 (RED → GREEN)
- 모드별 분기 처리
- 브랜드명 치환 로직

#### **Phase 4**: 통합 및 리팩터링
- 전체 시스템 통합
- 코드 품질 개선

## 🚨 AI 업무 방법 가이드 준수 (Phase 0 체크)

### **업무 성격 분석**: Frontend-Backend 상호작용 관련
- **작업 내용**: 웹뷰에서 백엔드 메시지 i18n 필터링
- **해당 분야**: UI 설정, 상태 관리, 메시지 통신

### **필수 문서 체크 대상**
- `caret-docs/development/frontend-backend-interaction-patterns.mdx` ⚡ **MANDATORY**
- `caret-docs/development/caret-architecture-and-implementation-guide.mdx` (섹션 10-11)
- `caret-docs/development/message-processing-architecture.mdx`

### **아키텍처 결정**
- ✅ **Caret vs Cline**: 새 기능이므로 `webview-ui/src/caret/` 폴더 사용
- ✅ **백업 요구사항**: 백엔드 수정 없음, 웹뷰만 수정
- ✅ **테스트 파일**: `webview-ui/src/caret/**/*.test.ts` 위치

### **현재 상태**: Phase 0 ✅ 완료, TDD Phase 1 (RED) 진행 중

### **📋 TDD 진행 상황**

#### ✅ **Phase 0: 필수 문서 체크 완료**
- Frontend-Backend 상호작용 패턴 가이드 확인
- 아키텍처 결정: `webview-ui/src/caret/i18n/` 디렉토리 사용

#### 🔴 **Phase 1 (RED): 실패 테스트 작성 완료**
- 파일 생성: `/webview-ui/src/caret/i18n/__tests__/backend-message-filter.test.ts`
- **5개 테스트 그룹** 작성:
  1. 백엔드 메시지 패턴 매칭 (7개 테스트)
  2. i18n 키 매핑 (6개 테스트)  
  3. 브랜드 모드별 동작 (3개 테스트)
  4. 옵션 토글 동작 (3개 테스트)
  5. 에러 케이스 처리 (4개 테스트)
- **총 22개 테스트 케이스** 작성
- **현재 상태**: ✅ **모든 테스트 통과 (22/22)**

#### ✅ **Phase 2 (GREEN): 최소 구현 완료**
- 파일 생성: `/webview-ui/src/caret/i18n/backend-message-filter.ts`
- 파일 생성: `/webview-ui/src/caret/i18n/backend-message-mappings.json`
- **핵심 구현 완료**:
  - `isBackendMessage()`: 패턴 매칭으로 백엔드 메시지 식별
  - `getMessageType()`: 메시지 타입 분류
  - `translateBackendMessage()`: i18n 키 매핑 및 번역
  - `processBackendMessage()`: 메인 처리 함수
- **테스트 결과**: ✅ **22/22 통과** (100% 성공)

#### ✅ **Phase 3 (REFACTOR): 코드 품질 개선 완료**
- **타입 안전성 강화**: `SupportedLanguage`, `BrandMode`, `MessageType` 타입 정의
- **함수 문서화**: JSDoc으로 모든 공개 함수 문서화
- **코드 구조 개선**: 유틸리티 함수 분리 (`shouldAddBrandName`, `isValidMessage`)
- **에러 처리 강화**: 입력 검증 및 폴백 로직 개선
- **가독성 향상**: 변수명 개선, 주석 추가
- **최종 테스트**: ✅ **22/22 통과** (100% 유지)

## 🎯 **TDD 구현 최종 결과**

### ✅ **완성된 구현**
1. **핵심 파일**:
   - `/webview-ui/src/caret/i18n/backend-message-filter.ts` (242줄)
   - `/webview-ui/src/caret/i18n/backend-message-mappings.json` (38줄)
   - `/webview-ui/src/caret/i18n/__tests__/backend-message-filter.test.ts` (159줄)

2. **기능 커버리지**:
   - ✅ 7개 백엔드 메시지 패턴 지원
   - ✅ 한국어/영어 이중 언어 지원  
   - ✅ Cline/Caret 브랜드 모드 분기
   - ✅ 옵션 토글 (enableBackendI18n)
   - ✅ 에러 케이스 처리 (graceful fallback)

3. **사용법**:
```typescript
import { processBackendMessage } from '@/caret/i18n/backend-message-filter'

// Caret 모드에서 i18n 활성화
const translated = processBackendMessage("Cline wants to edit file.js", {
  mode: 'caret',
  enableBackendI18n: true,
  language: 'ko'
})
// 결과: "Caret이 파일을 편집하려고 합니다"

// Cline 모드에서는 원본 그대로
const original = processBackendMessage("Cline wants to edit file.js", {
  mode: 'cline'
})
// 결과: "Cline wants to edit file.js"
```

### 📊 **개발 성과**
- **TDD 사이클**: 완전한 RED-GREEN-REFACTOR 3단계 완주
- **단위 테스트**: 100% (22/22 테스트 통과)
- **코드 품질**: 타입 안전성, 문서화, 에러 처리 완비
- **아키텍처 준수**: Level 1 독립 모듈, 백엔드 무수정

### 🚧 **남은 작업 (새 방향)**
1. **i18n 자동 전환**: brand-converter.js에 i18n 파일 처리 기능 추가
2. **소스 통합**: 웹뷰 ClineMessage 렌더링에 필터 적용
3. **통합 테스트**: 실제 웹뷰에서 메시지 번역 및 브랜드 전환 확인
4. **모드 설정 연동**: caret-main의 Cline/Caret 모드 토글 이식
5. **E2E 테스트**: i18n 포함 전체 브랜드 전환 시스템 동작 확인

### ⚠️ **현재 상태 (2025-09-01 업데이트)**
- ✅ **백엔드 필터 완료**: i18n 필터링 시스템 단위 구현 (22/22 테스트)
- ✅ **CaretProvider 완료**: 브랜드/모드 분리 및 전역 상태 관리 시스템
- 🔄 **웹뷰 통합 작업**: ClineMessage 렌더링에 필터 적용
- ❌ **자동화 제거**: 복잡한 i18n 자동 브랜드 전환 시스템 제거 (수동 처리로 변경)

## 🎯 **단순화된 최종 목표**

### **핵심 작업 (남은 것)**
1. **웹뷰 통합**: processBackendMessage()를 실제 ClineMessage 컴포넌트에 적용
2. **CaretProvider 연동**: HostProvider.caret.*를 통한 모드별 동작 확인
3. **테스트**: 웹뷰에서 "Cline wants to edit" → "Caret이 파일을 편집하려고 합니다" 확인

### **제거된 복잡한 기능들**
- ❌ i18n 파일 자동 브랜드 전환
- ❌ --include-i18n 플래그 개발
- ❌ 하드코딩 텍스트 자동 변환 시스템
- ❌ 복잡한 품질 검증 도구 확장

**→ 이제 핵심 기능에만 집중하여 빠르게 완료 가능**

## 📋 i18n 자동 전환 대상 파일들 (brand-converter.js로 처리)
- `/webview-ui/src/App.tsx` - 하드코딩 "Cline" 텍스트 자동 변환
- `/webview-ui/src/components/welcome/WelcomeView.tsx` - 웰컴페이지 브랜드 텍스트 자동 변환
- `/webview-ui/src/context/ClineAuthContext.tsx` - 인증 컨텍스트 브랜드 텍스트 자동 변환
- `/webview-ui/src/components/welcome/HomeHeader.tsx` - 헤더 브랜드 텍스트 자동 변환
- 기타 i18n이 적용된 모든 파일의 하드코딩 텍스트

**자동 처리 방식**:
```bash
# i18n 포함 전체 브랜드 전환
node caret-b2b/tools/brand-converter.js caret forward --include-i18n

# 결과: 하드코딩 텍스트만 변환, i18n 키는 보존
# "Cline wants..." → "Caret wants..." (하드코딩 텍스트)
# t('welcome.title') → t('welcome.title') (i18n 키 보존)
```

---

## 🚨 **현황 분석: 과도한 설계로 인한 미사용 기능들** (2025-09-01)

### ⚠️ **중대 발견사항**: 구현된 기능들의 70%가 실제로 사용되지 않음

**오늘 작업 중 발견한 문제**:
- **계획된 복잡한 시스템들이 대부분 구현되었으나 실제 연결되지 않음**
- **3100줄 이상의 코드가 미사용 상태**
- **실제 필요한 작업은 단 2곳의 하드코딩 텍스트 수정뿐**

---

## 🚫 **사용되지 않는 기능들 상세 분석**

### **1. 백엔드 메시지 필터링 시스템** ❌ **완전 구현되었으나 100% 미사용**

#### **구현된 파일들**:
```
- webview-ui/src/caret/i18n/backend-message-filter.ts (242줄)
  → 메인 필터링 로직, processBackendMessage() 함수
- webview-ui/src/caret/i18n/backend-message-mappings.json (67줄)  
  → 7개 메시지 패턴 정의 (wants_to_edit, wants_to_create 등)
- webview-ui/src/caret/i18n/__tests__/backend-message-filter.test.ts (159줄)
  → TDD 완성, 22/22 테스트 통과 (100% 성공률)
```

#### **구현된 기능들**:
- ✅ "Cline wants to edit" → "Caret이 파일을 편집하려고 합니다" 번역
- ✅ 7개 메시지 패턴 지원 (edit, create, read, execute_command 등)
- ✅ 브랜드 모드별 동작 (Cline: 원본, Caret: 번역)
- ✅ 다국어 지원 (한국어/영어/일본어/중국어)
- ✅ 타입 안전성 (TypeScript 완벽 적용)
- ✅ 에러 처리 및 폴백 로직

#### **왜 사용되지 않는가**:
- `ChatRow.tsx`, `ExecuteCommandToolHandler.ts`에서 실제로 import하지 않음
- `processBackendMessage()` 함수 호출하는 곳이 전혀 없음
- **완벽한 구현체이지만 연결 누락**

---

### **2. 대규모 다국어 로케일 시스템** ❌ **30개 파일, 부분만 사용**

#### **구현 현황**:
```
총 30개 JSON 파일 (~2000줄):
- ko/ (한국어): 8개 파일 (common, settings, welcome, persona 등)
- en/ (영어): 8개 파일  
- ja/ (일본어): 7개 파일
- zh/ (중국어): 7개 파일
```

#### **실제 사용 상황**:
- ✅ **설정 페이지만 부분 사용** (ModeSystemToggle, UnifiedLanguageSetting)
- ❌ **대부분 미사용** (welcome.json, persona.json, workflow.json 등)
- ❌ **일본어/중국어 로케일 완전 미사용**

#### **과도한 설계 사례**:
```json
// ko/persona.json - 완전 미사용
{
  "aiPersona": {
    "title": "AI 페르소나",
    "description": "Caret의 성격과 말투를 설정합니다"
  }
}

// ko/workflow.json - 완전 미사용  
{
  "workflow": {
    "title": "워크플로우",
    "automation": "자동화 설정"
  }
}
```

---

### **3. CaretI18nProvider 시스템** ❌ **과도한 설계, 부분만 사용**

#### **구현된 파일들**:
```
- webview-ui/src/caret/context/CaretI18nContext.tsx (85줄)
  → React 컨텍스트 프로바이더
- webview-ui/src/caret/hooks/useCaretI18n.ts (45줄)
  → 커스텀 훅
- webview-ui/src/caret/utils/i18n.ts (120줄)
  → i18n 유틸리티 함수들
- webview-ui/src/caret/utils/lazy-i18n.ts (80줄)
  → 지연 로딩 시스템
- webview-ui/src/caret/utils/i18n-performance.ts (65줄)
  → 성능 모니터링 시스템
```

#### **과도한 기능들**:
- ❌ **지연 로딩**: 30개 파일을 동적으로 로드하는 복잡한 시스템
- ❌ **성능 모니터링**: i18n 렌더링 성능 측정 및 캐싱
- ❌ **전역 상태 관리**: React Context로 언어 상태 관리
- ✅ **실제 사용**: 설정 컴포넌트 몇 개에서만 부분 사용

---

### **4. 미완성된 자동화 시스템들** ❌ **계획만 있고 미구현**

#### **계획되었지만 구현하지 않은 기능들**:
```bash
# 품질 검증 도구 (계획만 있음)
node caret-b2b/tools/brand-config-validator.js --fix

# i18n 자동 브랜드 전환 (계획만 있음)  
node caret-b2b/tools/brand-converter.js caret forward --include-i18n

# caret-main UI 이식 (조사만 있음)
# CodeCenter 전용 메시지 번역 (설계만 있음)
```

#### **결과**:
- 복잡한 기획 문서만 작성됨
- 실제 구현은 하지 않음
- 문서와 실제 코드 간 격차 발생

---

## 📊 **미사용 코드 현황 통계**

### **라인 수 분석**:
```
백엔드 메시지 필터링: 468줄 (filter.ts + mappings.json + test.ts)
30개 로케일 파일: ~2000줄
CaretI18n 시스템: ~395줄 (5개 파일)
테스트 및 기타: ~300줄

총 미사용 코드: 약 3163줄 이상
```

### **파일 수 분석**:
```
완전 미사용 파일: 약 35-40개
부분 사용 파일: 약 5-8개  
실제 필요한 수정: 단 2개 파일의 각 1줄
```

---

## ✅ **실제 필요한 작업** (단순함)

### **문제 위치**:
1. `webview-ui/src/components/chat/ChatRow.tsx:308`
   ```typescript
   // 현재: 하드코딩
   <span>Cline wants to execute this command:</span>
   
   // 필요: 동적 브랜드명
   <span>{CaretProvider.brandName} wants to execute this command:</span>
   ```

2. `src/core/task/tools/handlers/ExecuteCommandToolHandler.ts:105`  
   ```typescript
   // 현재: 하드코딩
   `Cline wants to execute a command: ${command}`
   
   // 필요: 동적 브랜드명  
   `${CaretProvider.brandName} wants to execute a command: ${command}`
   ```

### **해결 방법**: 
- CaretProvider.brandName 사용하여 동적 브랜드명 적용
- 복잡한 i18n 번역 불필요
- 단순한 텍스트 치환으로 충분

---

## 🎯 **내일 작업 계획 및 현황 보고 가이드**

### 📋 **작업 시작 시 현황 보고 필수사항**

#### **1. 현재 상황 요약**:
```
❌ 문제: "Cline wants to execute this command" 하드코딩으로 브랜드 변환 안됨
✅ 원인: 2곳의 하드코딩 텍스트 (ChatRow.tsx + ExecuteCommandToolHandler.ts)
✅ 해결법: CaretProvider.brandName으로 동적 치환
❌ 발견: 3100줄+의 과도한 설계 코드가 실제로는 미사용 상태
```

#### **2. 완료된 작업들**:
- ✅ TerminalRegistry.ts 롤백 → extensionUri 오류 해결
- ✅ extension.ts 탭 제목 브랜드 변환 추가  
- ✅ 브랜드 변환 스크립트 개선 (3개 브랜드 지원)
- ✅ 미사용 기능들 상세 분석 완료

#### **3. 남은 작업 (단순함)**:
```
1. ChatRow.tsx:308 → 하드코딩 "Cline" → CaretProvider.brandName
2. ExecuteCommandToolHandler.ts:105 → 하드코딩 "Cline" → CaretProvider.brandName  
3. 테스트: 브랜드 변환 스크립트로 동작 확인
```

#### **4. 예상 소요시간**: 
- **작업**: 30분 이내 (단순한 텍스트 치환)
- **테스트**: 15분 이내 (브랜드 변환 확인)
- **총 소요시간**: 1시간 이내

---

### 📝 **내일 작업 프로세스**

#### **Step 1: 현황 보고** (5분)
```bash
# 현재 브랜드 상태 확인
node caret-b2b/tools/brand-converter.js --status

# 문제 재현 확인 
# "Cline wants to execute this command" 메시지가 여전히 Cline으로 출력되는지 확인
```

#### **Step 2: 작업 계획 수립** (5분)
```
1. ChatRow.tsx 수정 계획
2. ExecuteCommandToolHandler.ts 수정 계획  
3. 테스트 계획 (brand-converter.js로 브랜드 전환 후 확인)
```

#### **Step 3: 실제 작업 실행** (30분)
```
1. CaretProvider.brandName import 및 적용
2. 하드코딩 텍스트 → 동적 브랜드명 치환
3. 브랜드 변환 스크립트로 테스트
```

#### **Step 4: 완료 확인 및 정리** (20분)
```
1. Cline ↔ Caret 브랜드 전환 시 메시지 동작 확인
2. 문서 업데이트 (t03-2)
3. 미사용 코드 정리 여부 결정
```

---

### ⚠️ **중요 주의사항**

#### **작업 전 반드시 확인할 것**:
1. 현재 git status 및 브랜드 상태 확인
2. CaretProvider가 정상 동작하는지 확인
3. 기존 extension.ts, disk.ts 롤백이 유지되는지 확인

#### **작업 중 원칙**:
1. 단순한 텍스트 치환에만 집중
2. 복잡한 i18n 시스템은 건드리지 않음
3. 미사용 코드는 삭제하지 말고 그대로 둠 (향후 결정)

#### **완료 기준**:
- Caret 모드에서 "Caret wants to execute this command" 출력
- Cline 모드에서 "Cline wants to execute this command" 출력  
- brand-converter.js로 브랜드 전환 시 메시지도 함께 전환

---

## 📋 **최종 상태 요약**

### **✅ 성공적으로 구현된 핵심 기능들**:
1. CaretProvider 전역 브랜드 관리 시스템
2. 브랜드 변환 스크립트 (cline ↔ caret ↔ codecenter)  
3. 웹뷰 UI 언어 변경 시스템 (UnifiedLanguageSetting)
4. ModeSystemToggle (브랜드 전환 UI)

### **❌ 과도하게 설계된 미사용 기능들**:
1. 백엔드 메시지 필터링 시스템 (완전 구현, 완전 미사용)
2. 30개 다국어 로케일 파일들 (대부분 미사용)
3. CaretI18n 복잡한 시스템 (부분만 사용)  
4. 미완성된 자동화 도구들 (계획만 있음)

### **🎯 남은 핵심 작업**:
**단 2줄의 하드코딩 텍스트를 동적 브랜드명으로 교체하는 것**

이것이 전체 t03-2 프로젝트의 마지막 남은 작업입니다.

---

## 📊 **코드 제거 범위 분석 (2025-08-31)**

### **방법 1: 백엔드 메시지 필터링 (processUniversalBackendMessage) 제거 범위**

#### **완전 제거 대상 파일들** (cline-latest에서 복원 가능):
```
caret-src/i18n/backend-message-filter.ts (179 lines) - 완전 삭제
src/core/task/utils.ts:15-20 - processUniversalBackendMessage import/usage 제거
src/core/task/index.ts:876-879 - processUniversalBackendMessage 호출 제거
```

#### **cline-latest 복원 비교**:
- ✅ `src/core/task/utils.ts` - cline-latest와 동일한 상태로 복원 가능
- ✅ `src/core/task/index.ts` - cline-latest와 동일한 상태로 복원 가능  
- ❌ `caret-src/i18n/backend-message-filter.ts` - Caret 전용 파일, 단순 삭제

### **방법 2: 웹뷰 메시지 필터링 (processBackendMessage) 제거 범위**

#### **완전 제거 대상 파일들** (468 lines):
```
webview-ui/src/caret/i18n/backend-message-filter.ts (242 lines)
webview-ui/src/caret/i18n/backend-message-mappings.json (67 lines)
webview-ui/src/caret/i18n/__tests__/backend-message-filter.test.ts (159 lines)
webview-ui/src/caret/i18n/brand-toggle.ts:processBackendMessage 호출 부분
```

#### **TDD 테스트 상태**: 22/22 전체 통과, 완전 구현됨, 하지만 실제 연결 안됨

### **방법 3: brand-converter.js 통한 브랜드 교체 범위**

#### **추가 매핑 필요한 패턴들** (총 1708 occurrences 중 핵심 18개):
```javascript
// ChatRow.tsx 내 하드코딩된 메시지들 (18개 패턴)
"Cline wants to execute this command:" → "{{BRAND_NAME}} wants to execute this command:"
"Cline wants to edit this file:" → "{{BRAND_NAME}} wants to edit this file:"
"Cline wants to create a new file:" → "{{BRAND_NAME}} wants to create a new file:"
"Cline wants to read this file:" → "{{BRAND_NAME}} wants to read this file:"
"Cline wants to view the top level files" → "{{BRAND_NAME}} wants to view the top level files"
"Cline wants to recursively view all files" → "{{BRAND_NAME}} wants to recursively view all files"
"Cline wants to view source code definition names" → "{{BRAND_NAME}} wants to view source code definition names"
"Cline wants to search this directory for" → "{{BRAND_NAME}} wants to search this directory for"
"Cline wants to fetch content from this URL:" → "{{BRAND_NAME}} wants to fetch content from this URL:"
"Cline wants to start a new task:" → "{{BRAND_NAME}} wants to start a new task:"
"Cline wants to condense your conversation:" → "{{BRAND_NAME}} wants to condense your conversation:"  
"Cline wants to create a Github issue:" → "{{BRAND_NAME}} wants to create a Github issue:"
"Cline wants to use a tool" → "{{BRAND_NAME}} wants to use a tool"
"Cline wants to access a resource" → "{{BRAND_NAME}} wants to access a resource"
"Cline is using the browser:" → "{{BRAND_NAME}} is using the browser:"
"Cline wants to use the browser:" → "{{BRAND_NAME}} wants to use the browser:"

// src/core/task/index.ts 내 OS 알림 메시지들 (13개 패턴)
`Cline wants to ${fileExists ? "edit" : "create"}` → `{{BRAND_NAME}} wants to ${fileExists ? "edit" : "create"}`
`Cline wants to read ${path.basename(absolutePath)}` → `{{BRAND_NAME}} wants to read ${path.basename(absolutePath)}`
`Cline wants to view directory` → `{{BRAND_NAME}} wants to view directory`
`Cline wants to view source code definitions` → `{{BRAND_NAME}} wants to view source code definitions`
`Cline wants to search files` → `{{BRAND_NAME}} wants to search files`
`Cline wants to use a browser and launch` → `{{BRAND_NAME}} wants to use a browser and launch`
`Cline wants to execute a command:` → `{{BRAND_NAME}} wants to execute a command:`
`Cline wants to use ${tool_name} on ${server_name}` → `{{BRAND_NAME}} wants to use ${tool_name} on ${server_name}`
`Cline wants to access ${uri} on ${server_name}` → `{{BRAND_NAME}} wants to access ${uri} on ${server_name}`
"Cline wants to start a new task..." → "{{BRAND_NAME}} wants to start a new task..."
"Cline wants to condense the conversation..." → "{{BRAND_NAME}} wants to condense the conversation..."
"Cline wants to create a github issue..." → "{{BRAND_NAME}} wants to create a github issue..."
`Cline wants to fetch content from ${url}` → `{{BRAND_NAME}} wants to fetch content from ${url}`
```

#### **brand-converter.js 수정 필요사항**:
- 기존 단순 "Cline" → "{{BRAND_NAME}}" 매핑에서 위 31개 구체적 패턴 매핑으로 확장
- 템플릿 변수 치환 로직 추가 (`{{BRAND_NAME}}` → actual brand name)

### **🎯 권장 방법: 방법 4 (단순화된 브랜드명 교체 필터)**

#### **핵심 아이디어**: 
기존 `processUniversalBackendMessage` 구조 유지 + **다국어 번역 제거** + **단순 브랜드명 교체만**

#### **수정할 코드** (단 10줄):
```typescript
// caret-src/i18n/backend-message-filter.ts:126-165 수정
export function processUniversalBackendMessage(message: string, isOSNotification: boolean = false): string {
	if (!message || typeof message !== "string") {
		return message
	}

	try {
		const { CaretProvider } = require("../providers/CaretProvider")
		const { getCurrentBrandName } = require("../utils/brand-utils")
		
		const currentMode = CaretProvider.currentMode
		const brandName = getCurrentBrandName()

		// If in Cline mode, return original message
		if (currentMode === "cline") {
			return message
		}

		// Caret/CodeCenter mode: Simple brand replacement only
		return message.replace(/Cline/gi, brandName)
		
	} catch (error) {
		console.warn("Failed to process universal backend message:", error)
		return message.replace(/Cline/gi, "Caret")
	}
}
```

#### **제거할 코드**:
```typescript
// BACKEND_MESSAGE_PATTERNS 객체 전체 (lines 11-61) - 51 lines 삭제
// findMatchingPattern 함수 (lines 105-116) - 12 lines 삭제  
// processBackendNotificationMessage 함수 (lines 74-100) - 27 lines 삭제
// 복잡한 i18n 로직 (lines 147-155) - 9 lines 삭제
```

#### **결과**:
- **기존**: 179 lines → **수정 후**: ~80 lines (**99 lines 감소**)
- **제거 파일**: `webview-ui/src/caret/i18n/` 전체 (468 lines)
- **총 감소**: **567 lines**

#### **이유**:
1. **최소 변경**: 기존 연결된 코드 그대로 활용
2. **극도로 단순**: Cline → Caret/CodeCenter 교체만
3. **즉시 작동**: 별도 매핑 테이블 불필요
4. **안전성**: 에러 시 fallback 보장
5. **확장성**: 향후 필요시 다국어 쉽게 추가 가능

#### **구현 단계**:
1. `webview-ui/src/caret/i18n/` 전체 디렉토리 삭제 (468 lines)
2. `caret-src/i18n/backend-message-filter.ts` 단순화 (99 lines 감소)
3. 기존 연결 코드 유지 (`src/core/task/utils.ts`, `src/core/task/index.ts`)
4. 브랜드 전환 테스트 실행

---

## 🚀 **최종 구현 계획 (2025-08-31)**

### **Phase 1: 불필요한 코드 제거**
#### **1-1. cline-latest 비교 복원**
- **대상 파일**: `src/core/task/utils.ts`, `src/core/task/index.ts` 
- **방법**: cline-latest 버전과 비교하여 백엔드 메시지 교체 관련 코드인지 확인 후 복원
- **확인 기준**: `processUniversalBackendMessage` import/사용 코드만 제거, 나머지 Caret 기능은 유지

#### **1-2. 미사용 웹뷰 i18n 시스템 제거**
- **대상**: `webview-ui/src/caret/i18n/` 전체 디렉토리 (468 lines)
- **이유**: 완전 구현되었으나 실제 연결 안됨, TDD 22/22 통과하지만 사용되지 않음

### **Phase 2: 빌드 및 테스트 확인 (1차)**
- `npm run compile` 정상 빌드 확인
- `brand-converter.js` 실행: cline → caret 전환 테스트
- `brand-converter.js` 실행: caret → codecenter 전환 테스트  
- VS Code 확장 로드 및 기본 동작 확인

### **Phase 3: 단순화된 메시지 교체 구현**
#### **3-1. backend-message-filter.ts 단순화**
```typescript
// caret-src/i18n/backend-message-filter.ts 수정
export function processUniversalBackendMessage(message: string, isOSNotification: boolean = false): string {
	if (!message || typeof message !== "string") {
		return message
	}

	try {
		const { CaretProvider } = require("../providers/CaretProvider")
		const { getCurrentBrandName } = require("../utils/brand-utils")
		
		const currentMode = CaretProvider.currentMode
		const brandName = getCurrentBrandName()

		// Cline mode: return original message
		if (currentMode === "cline") {
			return message
		}

		// Caret/CodeCenter mode: Simple brand replacement (대소문자 모두)
		return replaceBrandInMessage(message, brandName)
		
	} catch (error) {
		console.warn("Failed to process universal backend message:", error)
		return replaceBrandInMessage(message, "Caret")
	}
}

// 새로운 유틸리티 함수 (caret 전용, brand-converter 변환 대상)
function replaceBrandInMessage(message: string, brandName: string): string {
	return message
		.replace(/Cline/g, brandName)     // 정확한 대소문자
		.replace(/cline/g, brandName.toLowerCase())  // 소문자
		.replace(/CLINE/g, brandName.toUpperCase())  // 대문자
}
```

#### **3-2. 제거할 코드**
- `BACKEND_MESSAGE_PATTERNS` 객체 (lines 11-61)
- `findMatchingPattern` 함수 (lines 105-116)  
- `processBackendNotificationMessage` 함수 (lines 74-100)
- 복잡한 i18n 번역 로직 (lines 147-155)

#### **3-3. 적용 범위**
- **OS 알림**: `showNotificationForApprovalIfAutoApprovalEnabled()` 메시지들
- **웹뷰 시스템 메시지**: ChatRow.tsx의 "wants to execute/edit/read" 등
- **에러 메시지**: backend에서 발생하는 모든 메시지

#### **3-4. brand-converter.js 연동**
- `replaceBrandInMessage` 함수를 brand-converter 변환 대상에 추가
- cline → caret → codecenter 전환 시 해당 로직도 함께 변환되도록 설정

### **Phase 4: 빌드 및 테스트 확인 (2차)**
#### **4-1. 기능 테스트**
- Cline 모드: "Cline wants to execute..." 정상 출력 확인
- Caret 모드: "Caret wants to execute..." 정상 출력 확인  
- CodeCenter 모드: "CodeCenter wants to execute..." 정상 출력 확인

#### **4-2. 브랜드 전환 테스트**
- `brand-converter.js cline caret`: 메시지도 함께 전환 확인
- `brand-converter.js caret codecenter`: 메시지도 함께 전환 확인
- `brand-converter.js codecenter cline`: 메시지 원복 확인

#### **4-3. 통합 테스트**
- OS 알림 메시지 브랜딩 확인
- 웹뷰 채팅 메시지 브랜딩 확인
- 에러 메시지 브랜딩 확인
- 모드 전환 시 메시지 즉시 변경 확인

### **예상 결과**
- **코드 감소**: 567 lines (468 + 99 lines)
- **복잡성 감소**: 다국어 시스템 → 단순 문자열 치환
- **안정성 증가**: fallback 메커니즘 보장
- **확장성 유지**: 향후 다국어 지원 쉽게 추가 가능
- **브랜드 일관성**: 모든 메시지에서 통일된 브랜드명 표시

### **성공 기준**
1. ✅ 빌드 에러 없음
2. ✅ cline ↔ caret ↔ codecenter 전환 정상 동작
3. ✅ OS 알림에서 올바른 브랜드명 표시
4. ✅ 웹뷰에서 올바른 브랜드명 표시  
5. ✅ 에러 발생 시에도 올바른 브랜드명 표시

---

**다음 작업 시작 시 반드시 이 현황 보고서를 참고하여 작업 계획을 세우시기 바랍니다.**