# (Deprecated) development/workflows

이 디렉터리는 과거에 `.agents/context`의 워크플로우/원자(Atoms)를 개발 문서 쪽에 “미러링”하기 위해 만들어졌습니다.

현재 Caret의 규칙/워크플로우 **Single Source of Truth**는 아래 경로입니다:
- `.agents/context/caret-rules.json`
- `.agents/context/workflows/*`
- `.agents/context/workflows/atoms/*`

개발자용 문서는 KO 우선으로 `caret-docs/development/index.md`에서 탐색합니다.

