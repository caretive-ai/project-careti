# Luke Yang - t03-3 프론트엔드 i18n 및 상호이식 개선 (상위 문서)

**작업 기간**: 2025-09-02 ~ 예정  
**담당자**: Luke Yang  
**우선순위**: Medium  
**AI 어시스턴트**: Claude Code  
**상위 작업**: [t03 브랜딩 시스템 구현](./t03-branding-ui.md)의 3단계  
**선행 작업**: [t03-2 백엔드 메시지 브랜딩](./t03-2-백엔드i18n페이지.md) 완료 후

## 📋 하위 작업 분할

### **단계별 작업 구조**
이 작업은 복잡성과 규모를 고려하여 다음과 같이 분할되었습니다:

- **[t03-3-1: 파일 교체 중심 i18n 이관](./t03-3-1-파일교체중심i18n이관.md)** ⏳ 진행중  
  - ✅ **전략 확정**: caret-main 완성본 → caret-merging 교체 방식
  - ChatRow, BrowserSessionRow 등 핵심 컴포넌트 우선 교체
  - 완성된 i18n 시스템 직접 활용으로 작업 효율성 극대화
  
- **[t03-3-2: 전체 텍스트 i18n 변환](./t03-3-2-전체텍스트i18n변환.md)** ⏸️ 대기
  - 200개+ 모든 하드코딩 텍스트 변환
  - 7개 네임스페이스 완성 (common, chat, settings, history, mcp, features, welcome)
  - 4개 언어 완전 지원

### **⚠️ 2025-09-02 최신 현황 업데이트**
- **빌드 상태**: ✅ 정상 (`npm run compile` 성공)
- **Git 상태**: ✅ 깨끗함 (이전 세션 코드 변경사항 모두 초기화됨)
- **실제 진행도**: **Phase 1 작업 전 단계** (문서화만 완료)

### **🔍 caret-merging 현재 i18n 구현 상태 확인 완료**
#### ✅ **이미 구현된 i18n 시스템**
- **locale 파일들**: `webview-ui/src/caret/locale/` (30개 JSON 파일)
  - ko/en/ja/zh 4개 언어 지원
  - common, settings, welcome 등 7개 네임스페이스
- **i18n 유틸리티**: `webview-ui/src/caret/utils/i18n.ts` (완전 구현)
- **다국어 설정 UI**: `PreferredLanguageSetting.tsx` (16개 언어 지원)

#### ❌ **아직 미적용된 부분**
- **하드코딩 텍스트**: ChatRow, BrowserSessionRow 등 여전히 `"Cline wants to..."`
- **i18n 연결**: 기존 컴포넌트들이 i18n 시스템과 연결되지 않음

### **🎯 수정된 핵심 발견사항**
- **caret-merging**: i18n 인프라는 완성, **활용만 안됨**
- **필요한 작업**: 완성된 파일 교체가 아닌 **기존 i18n 활용**
- **새로운 전략**: 무작정 이식하지 말고 **현재 시스템 기반 개선**

## 🎯 작업 목적

### **왜 이 작업이 필요한가?**
- 프론트엔드에 하드코딩된 "Cline" 텍스트들이 브랜드 변환되지 않음
- caret-main에서 이미 구현된 다국어 지원 웰컴페이지를 활용 가능
- 디자인 변경된 페이지들을 효율적으로 이식 필요

### **이 작업의 목표**
- 프론트엔드 i18n 적용 및 브랜드 변환 가능하게 만들기
- caret-main의 웰컴페이지 및 다국어 기능 이식
- brand-converter.js로 프론트엔드도 일괄 변환 가능하게

## ⚠️ 초기 시도의 실수와 교훈 (2025-09-02)

### **왜 잘못 이해했는가?**

#### **1. 브랜드 변환의 범위 오해**
- **잘못된 이해**: "브랜드명만 변환하면 된다" → 15개 "Cline" 텍스트만 변환
- **올바른 이해**: "모든 하드코딩된 텍스트를 i18n으로 변환" → 수백 개의 텍스트

#### **2. 변수 처리 방식의 오류**
- **잘못된 시도**: `${t('CARET.BRAND')} wants to...` (템플릿 리터럴)
- **문제점**: JSON은 정적 텍스트만 지원, 동적 변수 처리 불가
- **올바른 방식**: JSON 파일 내에서 완전한 문장으로 브랜드명 포함
  ```json
  // 잘못된 방식 (JSON에서 불가능)
  "message": "${BRAND} wants to execute"
  
  // 올바른 방식
  "clineWantsToExecute": "Caret wants to execute this command"
  ```

#### **3. 브랜드 전환 스크립트 역할 혼동**
- **잘못된 이해**: 프론트엔드 스크립트가 브랜드명 치환 담당
- **올바른 이해**: 
  - cline→caret: 스크립트가 하드코딩을 i18n 키로 변환
  - caret↔codecenter: locale 폴더 전체를 교체

#### **4. 아키텍처 레벨 혼동**
- **혼동**: 브랜드 변환을 코드 레벨에서 처리하려 함
- **명확화**: 
  - 코드 레벨: 하드코딩 → i18n 변환만
  - 파일 레벨: 브랜드별 locale 폴더 교체

### **교훈**
1. **i18n은 정적 시스템**: JSON 파일은 정적 텍스트만 지원
2. **브랜드는 데이터 영역**: 코드가 아닌 JSON 파일에서 관리
3. **명확한 책임 분리**: 코드 변환 vs 파일 교체 구분
4. **전체 텍스트 i18n화**: 브랜드명뿐 아니라 모든 UI 텍스트

## 📊 caret-merging 현재 상태 상세 분석 (2025-09-02)

### **✅ 이미 구현 완료된 i18n 시스템**

#### **1. 완전한 locale 파일 구조**
```
webview-ui/src/caret/locale/
├── ko/ (7개 JSON 파일) - 한국어
│   ├── announcement.json, common.json, models.json
│   ├── persona.json, settings.json, validate-api-conf.json
│   └── welcome.json
├── en/ (7개 JSON 파일) - 영어 
├── ja/ (8개 JSON 파일) - 일본어 (rules.json 추가)
└── zh/ (8개 JSON 파일) - 중국어 (rules.json 추가)
```
**상태**: ✅ **완전 구현됨** (총 30개 JSON 파일)

#### **2. 완전한 i18n 유틸리티 시스템**
```
webview-ui/src/caret/utils/
├── i18n.ts                 # ✅ 완전 구현 (30개 JSON import)
├── i18n-performance.ts     # ✅ 성능 모니터링 완성  
├── lazy-i18n.ts           # ✅ 지연 로딩 지원
└── CaretWebviewLogger.ts  # ✅ 로깅 유틸리티
```

#### **3. 완전한 다국어 설정 UI**
- **PreferredLanguageSetting.tsx**: ✅ 16개 언어 지원 완성
- **한국어 지원**: `Korean - 한국어` 옵션 완전 구현
- **UI 연동**: ExtensionState와 완전 연결됨

### **❌ 누락된 부분: i18n 시스템 활용**
현재 **i18n 인프라는 완성**되었지만 **실제 사용이 안됨**:
- ChatRow, BrowserSessionRow 등이 여전히 하드코딩 상태
- `t("key", "namespace")` 함수 호출이 컴포넌트에 연결되지 않음
- 완성된 locale 파일들이 활용되지 않음

#### **3. 현재 하드코딩된 "Cline" 텍스트 위치 파악**
- **ChatRow.tsx**: 
  - "Cline wants to..."
  - "Cline is having trouble..."
  - "Cline viewed..."
- **BrowserSessionRow.tsx**: 
  - "Cline is using the browser"
  - "Cline wants to use the browser"
- **Announcement.tsx**: 
  - "Cline is now optimized..."
- **AutoApproveModal.tsx**:
  - "Cline will automatically make..."
- **ClineRulesToggleModal.tsx**:
  - "Manage Cline Rules"
- **ApiOptions.tsx**:
  - Provider option "Cline"

### **🔍 기술적 현황 분석**

#### **브랜딩 전환 아키텍처**
1. **백엔드 (brand-config.json)**
   - 49개 TypeScript 파일에 대한 매핑
   - 단순 텍스트 치환 방식
   - 이미 구현 완료 (t03-2)

2. **프론트엔드 (brand-config-front.json - 신규)**
   - i18n 시스템과 연동 필요
   - 3단계 브랜딩:
     - cline: 하드코딩 영어
     - caret: i18n 다국어 지원
     - codecenter: i18n + 기업 브랜딩

#### **3자 비교를 위한 코드베이스**
- **cline-latest/**: 원본 Cline (i18n 미적용)
- **caret-merging/**: 현재 작업 중 (i18n 부분 적용)
- **caret-main/**: 완전한 i18n 구현 (참조용)

## 🔧 구현 전략

### **핵심 접근법: 2단계 브랜드 변환**

#### **❌ 잘못된 이해 (2025-09-02 초기 시도)**
처음에는 브랜드명을 코드에서 변수로 처리하려 했습니다:
- `"Cline wants to"` → `${t('CARET.BRAND')} wants to` (잘못된 접근)
- 브랜드를 동적 변수로 처리하면 JSON 파일에서 템플릿 리터럴 사용 필요
- JSON은 템플릿 리터럴을 지원하지 않음

#### **✅ 올바른 이해**
브랜드 변환은 두 가지 독립적인 프로세스입니다:

#### **Stage 1: cline ↔ caret (하드코딩 → i18n 변환)**
- **목적**: 하드코딩된 모든 텍스트를 i18n 키로 대체
- **예시**: 
  ```tsx
  // Before (cline - 하드코딩)
  "Cline wants to execute this command"
  
  // After (caret - i18n)
  t('chat.clineWantsToExecute')
  ```
- **중요**: 브랜드명은 JSON 파일 내에서 관리
  ```json
  // en/chat.json
  {
    "clineWantsToExecute": "Caret wants to execute this command"
  }
  ```

#### **Stage 2: caret ↔ codecenter (locale 폴더 교체)**
- **목적**: 브랜드별로 다른 locale 폴더 사용
- **방식**: 폴더 전체를 교체 (파일 수준 작업)
- `caret-b2b/brands/caret/locale/` → `webview-ui/src/caret/locale/`
- `caret-b2b/brands/codecenter/locale/` → `webview-ui/src/caret/locale/`
- **중요**: 코드는 동일, JSON 파일만 교체

#### **Phase 1: 최소 침습적 i18n 통합**
1. **Level 1 아키텍처 유지**
   - `/src/caret/` 폴더 내 독립적 i18n 시스템
   - 기존 Cline 코드 최소 수정
   - CARET MODIFICATION 주석으로 추적

2. **i18n 키 기반 렌더링**
   ```typescript
   // 예시: ChatRow.tsx 변환 전 (cline)
   <span>Cline wants to execute this command:</span>
   
   // 변환 후 (caret - i18n 적용)
   import { t } from '@/caret/utils/i18n'
   <span>{t('chat.clineWantsToExecute')}</span>
   
   // locale/en/chat.json (caret 브랜드)
   {
     "clineWantsToExecute": "Caret wants to execute this command"
   }
   
   // locale/en/chat.json (codecenter 브랜드 - 별도 폴더)
   {
     "clineWantsToExecute": "CodeCenter wants to execute this command"
   }
   ```

3. **i18n 통합 전략**
   - 모든 하드코딩 텍스트를 i18n 키로 대체
   - locale JSON 파일에서 브랜드별 텍스트 관리
   - brand-converter.js가 코드 변환 처리

#### **Phase 2: 브랜드별 설정 파일 구조**

##### **frontend-text-mappings.json (하드코딩 → i18n 변환용)**
```json
{
  "description": "프론트엔드 하드코딩 텍스트를 i18n 키로 변환",
  "replacements": {
    "webview-ui/src/components/chat/ChatRow.tsx": {
      "import": "import { t } from '@/caret/utils/i18n'",
      "changes": [
        {
          "from": "\"Cline wants to execute this command:\"",
          "to": "t('chat.clineWantsToExecute')"
        },
        {
          "from": "\"Cline is having trouble with this file:\"",
          "to": "t('chat.clineHavingTrouble')"
        },
        {
          "from": "\"Cline viewed images\"",
          "to": "t('chat.clineViewedImages')"
        }
      ]
    },
    "webview-ui/src/components/chat/BrowserSessionRow.tsx": {
      "import": "import { t } from '@/caret/utils/i18n'",
      "changes": [
        {
          "from": "\"Cline is using the browser:\"",
          "to": "t('chat.clineUsingBrowser')"
        },
        {
          "from": "\"Cline wants to use the browser:\"",
          "to": "t('chat.clineWantsToUseBrowser')"
        }
      ]
    }
  }
}
```

##### **brand-config-caret-to-codecenter.json (파일 교체용)**
```json
{
  "from": "caret",
  "to": "codecenter",
  "files": {
    "locale": {
      "source": "caret-b2b/brands/codecenter/original/locale/",
      "target": "webview-ui/src/caret/locale/",
      "backup": "caret-b2b/brands/caret/backup/locale/"
    },
    "i18n": {
      "source": "caret-b2b/brands/codecenter/original/utils/i18n.ts",
      "target": "webview-ui/src/caret/utils/i18n.ts",
      "backup": "caret-b2b/brands/caret/backup/utils/i18n.ts"
    }
  }
}
```

#### **Phase 3: 자동 변환 스크립트 분리**
```bash
# cline ↔ caret: 코드 레벨 변환
node brand-converter.js frontend cline-to-caret  # TSX 파일 수정
node brand-converter.js frontend caret-to-cline  # TSX 파일 복원

# caret ↔ codecenter: 파일 교체 방식
node brand-converter.js locale caret-to-codecenter  # locale 폴더 전체 교체
node brand-converter.js locale codecenter-to-caret  # locale 폴더 전체 복원
```

## 🚨 중요 작업 지침 (2025-09-02)

### **⚠️ 필수 준수사항**
1. **빌드 안되면 즉시 중지**: `npm run compile` 실패 시 원인 찾을 때까지 작업 중단
2. **무작정 이식 금지**: 현재 caret-merging에 이미 i18n 구현되어 있음 확인
3. **기존 시스템 우선 활용**: 새로 복사하기 전에 현재 구현 상태 점검
4. **단계별 검증**: 각 변경 후 반드시 컴파일 및 테스트 확인

## 📝 수정된 작업 계획

### **Phase 1: 현재 i18n 시스템 활용** ⏳ 진행중
**목표**: 이미 구현된 i18n 인프라를 활용하여 하드코딩 텍스트만 변환

#### **1-1. 현재 시스템 확인** ✅ 완료 (2025-09-02)
- [x] i18n 인프라 존재 확인 (`webview-ui/src/caret/utils/i18n.ts`)
- [x] locale 파일들 존재 확인 (30개 JSON 파일)
- [x] 다국어 설정 UI 확인 (`PreferredLanguageSetting.tsx`)
- [x] 하드코딩 텍스트 위치 재확인

#### **1-2. 기존 i18n 연결 작업** ⏸️ 대기
⚠️ **빌드 안되면 즉시 중지하고 원인 파악**
- [ ] ChatRow.tsx i18n 연결 테스트
  - 현재: `"Cline wants to execute this command:"`
  - 목표: `t("chat.executeCommand", "common")` 활용
  - ✅ **컴파일 확인 필수**: 변경 후 `npm run compile`
- [ ] BrowserSessionRow.tsx i18n 연결
  - 현재: `"Cline is using the browser:"`
  - 목표: 기존 i18n 키 활용
  - ✅ **컴파일 확인 필수**: 변경 후 `npm run compile`

#### **1-3. Settings 컴포넌트 i18n 연결** ⏸️ 대기
- [ ] 현재 settings i18n 구현 상태 점검
- [ ] 필요한 경우에만 추가 i18n 적용
- [ ] ✅ **컴파일 확인 필수**: 각 변경 후 빌드 테스트

#### **1-4. 브랜드 변환 테스트** ⏸️ 대기
- [ ] 현재 i18n 시스템으로 브랜드 전환 테스트
- [ ] caret ↔ codecenter 변환 가능성 확인
- [ ] ✅ **최종 검증**: 전체 시스템 컴파일 및 동작 확인

---

### **Step 2: 모든 하드코딩 텍스트 i18n 변환** ⏸️ 대기
**목표**: 브랜드 외 모든 UI 텍스트 i18n 적용 (200개+)

#### **2-1. 텍스트 매핑 확장**
- [ ] 모든 하드코딩 텍스트 목록 작성
- [ ] i18n 키 네이밍 규칙 정의
- [ ] `frontend-text-mappings.json` 업데이트

#### **2-2. 대량 변환 작업**
- [ ] 컴포넌트별 변환 (chat, settings, history, mcp 등)
- [ ] 누락된 i18n 키 JSON 파일에 추가
- [ ] 타입 정의 업데이트

#### **2-3. 검증 및 커밋**
- [ ] 전체 컴파일 테스트
- [ ] UI 렌더링 확인
- [ ] **커밋**: "feat: Complete frontend i18n implementation (Step 2)"
- [ ] **푸시**: 원격 저장소에 반영

---

### **Step 3: 브랜드별 locale 파일 관리 체계 구축** ⏸️ 대기
**목표**: caret ↔ codecenter 브랜드 전환을 위한 파일 교체 시스템

#### **3-1. 브랜드별 locale 디렉토리 구성**
- [ ] `caret-b2b/brands/caret/locale/` 생성
- [ ] `caret-b2b/brands/codecenter/locale/` 생성
- [ ] 각 브랜드별 JSON 파일 준비

#### **3-2. 파일 교체 스크립트 개발**
- [ ] `brand-converter-locale.js` 생성
- [ ] 백업/복원 메커니즘 구현
- [ ] 자동 교체 테스트

#### **3-3. 검증 및 커밋**
- [ ] caret ↔ codecenter 전환 테스트
- [ ] **커밋**: "feat: Add locale file swap system for brands (Step 3)"
- [ ] **푸시**: 원격 저장소에 반영

---

### **Step 4: 웰컴페이지 이식 (선택사항)** ⏸️ 대기
**목표**: caret-main의 웰컴페이지 통합

#### **4-1. 웰컴페이지 분석**
- [ ] caret-main 웰컴페이지 구조 파악
- [ ] 필요한 컴포넌트 식별
- [ ] 의존성 확인

#### **4-2. 컴포넌트 이식**
- [ ] WelcomeView.tsx 이식
- [ ] 관련 i18n 파일 통합
- [ ] 라우팅 연결

#### **4-3. 검증 및 커밋**
- [ ] 웰컴페이지 동작 확인
- [ ] **커밋**: "feat: Import welcome page from caret-main (Step 4)"
- [ ] **푸시**: 원격 저장소에 반영

---

## 📋 웰컴페이지 이식 상세 계획 (참고용)

> **참고**: 아래 내용은 웰컴페이지 이식 시 참고할 수 있는 기존 계획입니다.

### 핵심 취지
- **caret-main 기능 활용**: 기존에 구현된 i18n 웰컴페이지 시스템을 caret-merging으로 이식
- **자동 브랜딩 전환**: i18n 적용 페이지의 하드코딩된 텍스트를 자동으로 cline ↔ caret ↔ codecenter 전환
- **업스트림 호환성**: cline 원본과 최대한 호환되면서도 브랜딩 기능 제공

### 핵심 방향
1. **Level 1 독립 모듈**: 웹뷰의 `/src/caret/` 폴더 활용으로 충돌 최소화
2. **주석 기반 추적**: `// CARET MODIFICATION:` 표준으로 변경사항 표시
3. **자동 복구 시스템**: 브랜드별 하드코딩 텍스트 자동 저장/복원 메커니즘

## 📋 주요 작업 항목

### 🔧 3.1 caret-main 웰컴페이지 분석 및 이식 준비

#### **A. 웰컴페이지 구조 분석**
```
목표: caret-main의 i18n 웰컴페이지 컴포넌트 구조 파악
- 웰컴페이지 컴포넌트 파일 위치 확인
- i18n 키/값 매핑 구조 분석
- 브랜드별 차별화 요소 식별
- 기존 다국어 지원 범위 확인
```

#### **B. 이식 대상 컴포넌트 우선순위**
```
우선순위 1: WelcomeView 메인 컴포넌트
우선순위 2: HomeHeader 헤더 컴포넌트
우선순위 3: 기타 i18n 적용 페이지들
```

#### **C. 의존성 분석**
```
- caret-main의 i18n 라이브러리 버전 확인
- 웹뷰 환경 호환성 검토
- caret-merging에 필요한 추가 패키지 식별
```

### 🔧 3.2 i18n 페이지 자동 cline 전환 시스템 개발

#### **A. 하드코딩 텍스트 자동 감지**
```javascript
// 구현 목표: 하드코딩된 브랜드명 자동 감지 및 매핑
class I18nBrandTextDetector {
  detectHardcodedBrandText(filePath: string): HardcodedText[]
  createI18nKeyMapping(hardcodedTexts: HardcodedText[]): I18nKeyMapping
  generateAutoReplacementRules(): ReplacementRule[]
}
```

#### **B. 브랜드별 백업/복원 시스템**
```typescript
// 목표: 브랜드 전환 시 하드코딩 텍스트 자동 백업/복원
interface BrandTextBackup {
  brand: 'cline' | 'caret' | 'codecenter'
  filePath: string
  originalTexts: Record<string, string>  // 원본 하드코딩 텍스트
  i18nKeys: Record<string, string>       // 대응되는 i18n 키
}
```

#### **C. 자동 전환 스크립트 확장**
```
목표: brand-converter.js에 i18n 페이지 처리 기능 추가
- 하드코딩 텍스트 → i18n 키 자동 치환
- 브랜드별 백업 파일 관리
- 자동 복구 기능 (전환 실패 시 rollback)
```

### 🔧 3.3 웰컴페이지 통합 구현

#### **A. 컴포넌트 이식**
```
/webview-ui/src/caret/pages/welcome/
├── WelcomeView.tsx          # 메인 웰컴페이지
├── HomeHeader.tsx           # 헤더 컴포넌트
├── welcome.i18n.json       # 웰컴페이지 전용 i18n
└── __tests__/               # 컴포넌트 테스트
```

#### **B. 브랜드 토글 시스템 연동**
```typescript
// 목표: 기존 brand-toggle.ts와 연동
import { getCurrentBrandConfig, isI18nEnabled } from '@/caret/i18n/brand-toggle'

// Cline 모드: 하드코딩 영어 텍스트
// Caret 모드: i18n 한국어/영어 지원
// CodeCenter 모드: 기업 브랜딩 + i18n
```

#### **C. 라우팅 및 네비게이션**
```
목표: 웰컴페이지를 기존 웹뷰 구조에 통합
- 웰컴페이지 표시 조건 정의
- 기존 페이지와의 네비게이션 연결
- 브랜드별 웰컴 콘텐츠 차별화
```

### 🔧 3.4 자동 전환 스크립트 검증

#### **A. 브랜드 전환 테스트**
```bash
# 웰컴페이지 포함한 전체 브랜드 전환 검증
node caret-b2b/tools/brand-converter.js caret forward --include-i18n-pages
node caret-b2b/tools/brand-converter.js caret backward --include-i18n-pages
node caret-b2b/tools/brand-converter.js codecenter forward --include-i18n-pages
node caret-b2b/tools/brand-converter.js codecenter backward --include-i18n-pages
```

#### **B. 자동 복구 시스템 테스트**
```
테스트 시나리오:
1. 정상 전환 → 복구
2. 전환 실패 → 자동 롤백
3. 백업 파일 손실 → 안전 모드
4. i18n 키 누락 → 폴백 처리
```

#### **C. TDD 테스트 확장**
```javascript
describe('I18n Pages Auto Conversion', () => {
  test('should detect hardcoded brand texts in welcome pages')
  test('should create backup before brand conversion')
  test('should restore backup on conversion failure') 
  test('should handle missing i18n keys gracefully')
  test('should support cline ↔ caret ↔ codecenter conversion')
})
```

## 🚨 중요 제약사항 및 원칙

### **Luke 지시사항 준수**
- **"i18n은 자동 전환 대상 아님"**: i18n 키 자체는 변경하지 않고 하드코딩 텍스트만 처리
- **"주석 잘 달기"**: 모든 변경에 `// CARET MODIFICATION: 웰컴페이지 브랜드 토글` 형식 주석
- **단계별 확인**: 각 주요 단계마다 결과 확인 후 다음 진행

### **기술적 제약**
- **업스트림 머징 고려**: Cline 원본 파일 최소 수정
- **Level 1 독립성**: 웹뷰 `/src/caret/` 폴더 우선 활용
- **백업 의무**: 변경 전 반드시 `.cline` 백업 생성

### **품질 기준**
- **TDD 필수**: 모든 새 기능은 테스트 우선 개발
- **타입 안전성**: TypeScript 엄격 모드 준수
- **에러 처리**: Graceful fallback으로 시스템 안정성 보장

## 🧪 TDD 개발 계획

### **Phase 1**: 웰컴페이지 컴포넌트 분석 (RED)
```javascript
describe('Welcome Page Component Analysis', () => {
  test('should identify caret-main welcome components')
  test('should extract i18n key mappings')
  test('should detect hardcoded brand texts')
})
```

### **Phase 2**: 자동 전환 스크립트 확장 (GREEN)
```javascript  
describe('Auto Conversion Script Extension', () => {
  test('should backup hardcoded texts before conversion')
  test('should convert hardcoded texts to i18n keys')
  test('should restore from backup on failure')
})
```

### **Phase 3**: 웰컴페이지 통합 (REFACTOR)
```javascript
describe('Welcome Page Integration', () => {
  test('should render welcome page with brand toggle')
  test('should switch between cline/caret/codecenter modes')
  test('should handle i18n fallbacks correctly')
})
```

## 📊 예상 결과물

### **완성된 파일 구조**
```
/webview-ui/src/caret/pages/welcome/
├── components/
│   ├── WelcomeView.tsx
│   ├── HomeHeader.tsx  
│   └── BrandModeSelector.tsx
├── i18n/
│   ├── welcome.ko.json
│   ├── welcome.en.json
│   └── welcome.codecenter.json
├── __tests__/
│   ├── WelcomeView.test.tsx
│   └── brand-conversion.test.ts
└── index.ts

/caret-b2b/tools/
└── brand-converter.js          # i18n 페이지 처리 기능 추가
```

### **새로운 기능**
1. **웰컴페이지 브랜드 토글**: Cline/Caret/CodeCenter 모드별 차별화
2. **하드코딩 텍스트 자동 감지**: 브랜드명 자동 발견 및 매핑
3. **자동 백업/복원**: 전환 실패 시 안전한 롤백
4. **확장된 브랜딩 스크립트**: i18n 페이지 포함 전체 브랜드 전환

### **사용법 (완성 후)**
```bash
# i18n 페이지 포함 전체 브랜드 전환
node caret-b2b/tools/brand-converter.js caret forward --include-i18n-pages

# 웰컴페이지만 브랜드 전환 (개발/테스트용)
node caret-b2b/tools/brand-converter.js caret forward --only-welcome-pages

# 자동 백업 시스템 확인
node caret-b2b/tools/brand-converter.js --check-backups
```

## 📝 요약

### **구현 전략**
1. **cline → caret**: 하드코딩 텍스트를 i18n 키로 변환 (TSX 파일 수정)
2. **caret ↔ codecenter**: locale + i18n.ts 파일 전체 교체 (파일 스왑)

### **설정 파일**
- `frontend-text-mappings.json`: 하드코딩 텍스트 매핑
- `locale-[brand].json`: 각 브랜드의 locale 파일 경로

### **주의사항**
- i18n.ts에 30개 JSON 파일 개별 import (확장성 문제)
- 각 브랜드가 독립적으로 파일 관리
- 백업은 매번 갱신 (overwrite: true)

## 🚀 즉시 실행 가능한 작업

### **1. brand-config-front.json 생성**
- 프론트엔드 전용 브랜딩 설정 파일
- 하드코딩 텍스트 → i18n 키 매핑
- JSON 파일 내 브랜드명 치환 규칙

### **2. 프론트엔드 코드 변환**
```bash
# brand-converter.js 실행으로 자동 변환
node caret-b2b/tools/brand-converter.js frontend apply

# 변환 내용:
# 1. 하드코딩 텍스트 → i18n 키 대체
# 2. import 문 자동 추가
# 3. locale JSON 파일 생성/업데이트
```

### **3. 브랜드 변환 패턴**

#### **cline → caret (코드 변환)**
```typescript
// Before (cline)
<span>Cline wants to execute this command:</span>

// After (caret with i18n)
<span>{t('CARET.BRAND')} wants to execute this command:</span>
```

#### **caret → codecenter (파일 교체)**
```bash
# 브랜드별 파일 구조
caret-b2b/brands/
├── cline/
│   ├── original/             # Cline 원본 (하드코딩)
│   │   └── components/       # 하드코딩된 컴포넌트 백업
│   └── converted/            # Cline→Caret 변환 후 파일
│       ├── locale/           # 기본 영어 locale (BRAND: "Cline")
│       └── utils/i18n.ts    # i18n 시스템
├── caret/
│   ├── original/             # Caret 원본 (i18n 적용)
│   │   ├── locale/           # 다국어 locale (BRAND: "Caret")
│   │   │   ├── ko/*.json
│   │   │   ├── en/*.json
│   │   │   ├── ja/*.json
│   │   │   └── zh/*.json
│   │   └── utils/i18n.ts    # 30개 JSON import
│   └── backup/               # 현재 상태 백업
└── codecenter/
    ├── original/             # CodeCenter 원본
    │   ├── locale/           # 다국어 locale (BRAND: "CodeCenter")
    │   │   ├── ko/*.json
    │   │   ├── en/*.json
    │   │   ├── ja/*.json
    │   │   └── zh/*.json
    │   └── utils/i18n.ts    # 30개 JSON import (경로 수정)
    └── backup/               # 현재 상태 백업

# brand-converter.js 스크립트 동작

## cline → caret (코드 변환)
1. frontend-text-mappings.json 읽기
2. 각 파일에서 하드코딩 텍스트를 t('CARET.BRAND')로 치환
3. import 문 추가

## caret ↔ codecenter (파일 교체)
1. 현재 파일 백업 (backup/ 폴더에)
2. locale-[brand].json에서 경로 읽기
3. 해당 브랜드 파일로 교체
```

## 🔄 t03-2 완료 후 연결점

### **t03-2에서 받아올 것**
- `brand-toggle.ts`: 브랜드 모드 감지 및 i18n 활성화
- `backend-message-filter.ts`: 백엔드 메시지 i18n 처리
- 기존 i18n 시스템과의 연동 방법

### **t03-3에서 확장할 것** 
- 웰컴페이지 레벨에서의 브랜드 토글 UI
- 하드코딩 텍스트 자동 처리 시스템
- 브랜딩 스크립트의 i18n 페이지 지원

### **연결 방식**
```typescript
// t03-2의 brand-toggle.ts 확장
import { getCurrentBrandConfig } from '@/caret/i18n/brand-toggle'
import { processWelcomePageTexts } from '@/caret/pages/welcome'

// 웰컴페이지에서 브랜드별 텍스트 처리
const welcomeConfig = getCurrentBrandConfig()
const processedTexts = processWelcomePageTexts(welcomeConfig)
```

---

**작업 완료 조건**: 
1. ✅ 프론트엔드 하드코딩 텍스트가 i18n 시스템으로 대체
2. ✅ brand-config-front.json으로 프론트엔드 브랜딩 자동화
3. ✅ Cline/Caret/CodeCenter 3단계 브랜딩 완성
4. ✅ 웰컴페이지 포함 전체 UI 다국어 지원

## 📌 빠른 참조

### **주요 파일 위치**
```
# i18n 시스템
webview-ui/src/caret/utils/i18n.ts
webview-ui/src/caret/locale/**/*.json

# 브랜딩 설정
caret-b2b/brands/caret/brand-config.json (백엔드)
caret-b2b/brands/caret/brand-config-front.json (프론트엔드 - 생성 예정)

# 하드코딩 텍스트 위치
webview-ui/src/components/chat/*.tsx
webview-ui/src/components/settings/*.tsx
```

### **테스트 명령어**
```bash
# 프론트엔드 i18n 테스트
npm run test:webview

# 브랜드 변환 테스트 (프론트엔드)
node caret-b2b/tools/brand-converter.js caret forward --frontend
node caret-b2b/tools/brand-converter.js codecenter forward --frontend
```






