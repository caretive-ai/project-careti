# i18n 마이그레이션 작업 가이드

이 문서는 하드코딩된 텍스트를 i18n 키로 변환하는 작업을 위한 상세 가이드라인을 제공합니다.

## 작업 절차

### 1. `t` 함수 적용

-   파일 내 하드코딩된 모든 사용자 대상 문자열을 `t()` 함수로 감싸 i18n 키를 사용하도록 변경합니다.
-   **예시:** `"API Key"` -> `{t('settings.apiKeyField.apiKey')}`

### 2. `key` 명명 규칙

-   i18n 키는 다음 형식을 따릅니다: `[카테고리].[파일명].[상세내용]`
-   **카테고리:** `i18n-missing-files-report.md`에 명시된 카테고리 (예: `settings`, `chat`)
-   **파일명:** 확장자를 제외한 파일명 (예: `apiKeyField`)
-   **상세내용:** 문자열의 의미를 명확하게 나타내는 이름 (예: `apiKey`, `saveButton`)

### 3. `locale` JSON 파일 수정

-   새로 생성한 i18n 키와 해당 한글 번역을 `locales/ko/translation.json` 파일에 추가합니다.
-   JSON 파일의 구조는 `key` 명명 규칙과 동일하게 중첩된 형태로 작성합니다.
-   **예시:**
    ```json
    {
      "settings": {
        "apiKeyField": {
          "apiKey": "API 키"
        }
      }
    }
    ```

### 4. 테스트 확인

-   수정한 파일과 관련된 테스트 코드가 존재할 경우, 모든 테스트가 성공적으로 통과하는지 확인해야 합니다.
-   UI 텍스트를 찾는 테스트 코드가 있다면, i18n 키가 아닌 실제 렌더링될 텍스트를 기준으로 검증해야 할 수 있습니다.

### 5. 커밋 메시지 규칙

-   작업 완료 후 커밋 메시지는 다음 형식을 따릅니다.
-   `feat(i18n): [파일명] i18n 적용`
-   **예시:** `feat(i18n): ApiKeyField.tsx i18n 적용`
